# Nhật ký checkpoint

Append-only. **Không cần đọc file này để làm việc** — trạng thái hiện tại nằm ở
[`fe-migration-ledger.md`](../fe-migration-ledger.md), phát hiện kỹ thuật nằm ở
[`findings/`](findings/). Chỉ mở khi cần truy lại "checkpoint X hồi đó làm gì".
| ngày | task | nội dung |
| ---------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-22 | P0.1 + P0.2 | Boot BE, verify `/v3/api-docs` → 200. Sinh `src/core/api/schema.gen.ts` (90 path / 101 operation / 108 schema). |
| 2026-07-22 | P0.3 | Đối chiếu spec ↔ `src/lib/api/*`: 58 OK · 5 wire sai · 36 chưa có · 2 n/a · 7 FE thừa. |
| 2026-07-22 | P0.4 | Bản đồ route → domain (mục 2), chốt domain chủ của `/dashboard`, đo độ phủ UI (mục 3). |
| 2026-07-22 | P0.5 | `docs/design-tokens-map.md`. Phát hiện: app render bằng serif, dark mode là code chết, palette không có màu thương hiệu, alias DS đụng alias shadcn. |
| 2026-07-24 | P1.1 | Scaffold `core/` `shared/` `features/` + tsconfig paths. |
| 2026-07-24 | P1.2 | Move axios / Redux store / providers vào `core/`; tách `core/query/client.ts`. |
| 2026-07-24 | P1.3 | Transcribe token DS vào `globals.css` (tiền tố `nx-`), sửa lỗi font serif, mount `ThemeProvider` theo `data-theme`, dựng 3 primitive `Button` `Input` `Card`. |
| 2026-07-24 | P2.1a | `features/security/` types + api, chu kỳ 1/3 (session + OAuth, 8/8 endpoint). Phát hiện drift alarm của `schema.gen.ts` đang hỏng — xem mục 5. |
| 2026-07-24 | drift fix | `.prettierignore` + `springdoc.writer-with-order-by-keys` (BE) + reset baseline. Verify: sinh lại qua restart JVM ra file byte-identical, diff rỗng. |
| 2026-07-24 | P2.1b | `features/security/hooks/` — query key namespace, session establish/clear, `useLogin` `useRegister` `useLogout` `useOAuthUrl` `useOAuthCallback`. |
| 2026-07-24 | P2.1c-1 | UI login + register: `LoginForm` `RegisterForm` từ `shared/`, primitive `Avatar`, `shared/lib/api-error`, validation in-feature. Sửa thiếu `--radius-nx-full`. Verify preview: light/dark, validation, DS Input khớp specimen. |
| 2026-07-24 | P2.1c-2 | OAuth: `OAuthButtons` (Google/GitHub, prefetch URL) gắn vào login+register; `OAuthCallback` (đổi code→token). Verify preview: nút + divider, denied + exchange-error states, prefetch fires. Phát hiện env swap client*id — xem mục 6. |
| 2026-07-24 | P2.1d | Wire `/login` `/register` + route mới `/oauth/[provider]/callback` (compose feature). Bridge role-sync legacy ở route layer. Xoá legacy login+register (api/hooks/schema). `(auth)` layout bỏ gradient. `.env.local` (gitignored) trỏ FE→:8080. **Verify BE thật**: login `nguyen.truc@test.com/12345678`→/dashboard (role USER, redirect bridge chạy); sai mật khẩu→401 inline; oauth denied; provider lạ→404. |
| 2026-07-24 | P2.1'a | Recovery data layer (chu kỳ 2): `types/recovery.ts` + `api/recovery.ts` (`recoveryApi`, 5/5 endpoint). Drift check trước khi làm: diff `schema.gen.ts` rỗng. |
| 2026-07-24 | P2.1'b | Recovery state layer: `hooks/use-recovery.ts` — `useForgotPassword` `useResetPassword` `useVerifyEmail` `useRequestMagicLink` `useMagicLinkLogin` (reuse `useEstablishSession`). |
| 2026-07-24 | P2.1'c-1 | Recovery UI (chu kỳ 2, tách 1/3): `RequestLinkForm` dùng chung cho forgot-password + magic-link (variant prop). Thêm i18n `auth.magicLink`. Verify preview: 2 variant, submit thật→200→success state, light/dark. |
| 2026-07-24 | P2.1'c-2 | Recovery UI (tách 2/3): `VerifyEmailStatus` (terminal card) + `MagicLoginCallback` (session+redirect, twin của OAuthCallback). i18n `auth.verifyEmail`/`auth.magicLogin`. Verify BE thật: no-token→invalid, ?token=fake→401→error states (verify hiện msg BE, magic hiện invalid card); dark tokens remap. Screenshot không chụp được (pane không composite) — verify bằng page-text+network+computed style. |
| 2026-07-24 | P2.1'c-3 | Recovery UI (tách 3/3): `ResetPasswordForm` (token từ URL + mật khẩu mới + xác nhận, refine khớp). i18n `auth.resetPassword`. Verify BE thật: no-token→invalid; mismatch→lỗi client; submit khớp→401→error banner; light/dark. **Chu kỳ 2 UI xong.** |
| 2026-07-24 | P2.1'd | Wire 5 route recovery (forgot/magic-link → RequestLinkForm; reset/verify/magic-login → component tương ứng, Suspense; magic-login qua role bridge). Xoá legacy: `lib/schemas/auth.ts` (cả file), recovery fns/hooks trong `lib/api/auth.ts`+`lib/hooks/use-auth.ts` (còn mỗi logout cho shell). Verify BE thật: 5 route render, verify/magic-login fire→401. **Chu kỳ 2 xong.** |
| 2026-07-24 | P2.1"a | Profile data layer (chu kỳ 3): `types/profile.ts` + `api/profile.ts` (`profileApi`, 4/4). Nullability xác nhận từ Java: `username`+`profilePictureUrl` nullable, `role` luôn có. **types+api security xong 17/17.** Drift check rỗng. |
| 2026-07-24 | P2.1"b | Profile state layer: `hooks/use-profile.ts` — `useMyProfile` (query) + update/changePassword/changePicture (mutation, setQueryData patch). Query key `security.profile()` để role bridge chia sẻ ở 2"d. **hooks security xong 17/17.** |
| 2026-07-24 | P2.1"c | Profile UI: `ProfileIdentityCard` (avatar+upload+name/email), `ProfileInfoForm`, `ChangePasswordForm` từ shared/. i18n profile.\* (thêm saved/updated, sửa min 8→6). Verify BE thật (login seed): GET /profile/me→200 render, PUT /profile→200 'Đã lưu', mismatch mật khẩu→lỗi client; light/dark. **1 màn, security-owned; phần đa domain là P3.2.** |
| 2026-07-24 | P2.1"d | Wire /profile → 3 feature component; xoá legacy chỉ /profile dùng (3 hook edit trong use-user, cả file lib/schemas/user.ts). **GIỮ useProfile + use-admin-role + post-auth-redirect + lib/api/profile** — shell/dashboard/chats/posts còn dùng, chết ở P2.4/P2.7/P3.4. Verify BE thật: /profile render trong shell, GET /profile/me×2 (feature+shell key)→200. **SECURITY DOMAIN XONG (mọi surface).** |
| 2026-07-24 | P2.2a | friendships data layer: `features/friendships/{types,api}` (`friendshipApi`, 8/8 endpoint). Nullability từ Java: `nextCursor`/`username`/`profilePictureUrl` nullable; `friends` là list (non-null). Extraction test SẠCH (chỉ core/). Drift rỗng. |
| 2026-07-24 | P2.2b | friendships state layer: `hooks/use-friendship.ts` — friends (one-shot + infinite), suggestions, pending, sent + 4 mutation. Invalidate tường minh: accept→pending+friends, send→sent+suggestions, reject→pending, cancel→sent. Hooks không toast (UI lo). Bỏ mock/toUserSummary legacy. |
| 2026-07-25 | P2.4b | posts state layer chu kỳ 1/3: `hooks/` — `useCreatePost` `useCreateBookPost` `useUpdatePost` `useDeletePost` `useAcceptAnswer` `useSubmitQuiz` (mutation) + `useResolveLocation` (**query**, không phải mutation: resolve là read, Gemini chậm nên cache 10 phút, `retry:false`). Namespace `postKeys`. **Chốt cách invalidate xuyên domain**: posts KHÔNG đụng key của newsfeed/search — mọi mutation nhận `UseMutationOptions` để bên compose tự truyền, giữ chiều phụ thuộc newsfeed→posts. Chỉ `useAcceptAnswer` có invalidate nội domain (`postKeys.comments`, key đặt sẵn cho chu kỳ 2). |
| 2026-07-26 | P2.4c-3 | posts UI chu kỳ 1 (tách 3/5): ô nhập theo loại — `CodeSnippetFields` `ArticleFields` `QnaFields` `PollFields` `LinkFields` (**đúng trần 5 component**), `PostComposer` thêm bộ chuyển loại (5 pill + panel + nút bỏ). **Một state cho mỗi loại, chỉ gửi đúng 1 key details** vì `BeanUtils.copyProperties` không lọc theo `postType`. `Textarea` thêm prop `mono` (mirror `Input.mono`). i18n `createPost.{type,contentPlaceholder,code,article,qna,poll,link}` (en+vi). **Cắt `bountyPoints`** (deviation #12) và `QnaFields` gửi `qnaDetails:{isResolved:false}` vì `acceptAnswer` chặn khi object null. Verify BE thật: 5 POST /posts → 200, SQL xác nhận **đúng 1 cột details** được ghi mỗi bài, `endDate` lưu đúng instant; route preview tạm render cả 5 component, i18n không rò key, URL sai → lỗi inline; đã xoá route preview. Screenshot không chụp được (extension Chrome không kết nối) — verify bằng SSR markup + SQL. Phát hiện mới: **rule engine CÓ chặn đồng bộ** (rate limit 5 bài/phút, duplicate 60s) và **bài bị từ chối vẫn ghi content-hash** → gửi lại y nguyên trong 60s bị báo trùng dù chưa lưu gì; **không có endpoint vote poll**; **không có endpoint upload ảnh** ngoài book/avatar; `updatePost` copy cả field null → sửa bài là **ghi trắng** field không gửi. Xem mục 6. |
| 2026-07-27 | P2.4c-4 | posts UI chu kỳ 1 (tách 4/5): shared `Radio` (DS **có** spec, đã xem specimen render) + `BookPostFields` (multipart, kèm helper cục bộ `FileField` dùng 2 lần cho file/bìa — **không** đẻ primitive `FileInput`, xem mục 6) + `QuizComposer`. **3 component, dưới trần 5.** Composer nay 7/8 loại (thiếu `EVENT` → chu kỳ 3) và tách **2 endpoint**: `BOOK` đi `POST /posts/books`, còn lại đi `POST /posts`. **Quiz là đính kèm, không phải loại post** — nút riêng cạnh picker địa điểm. **CẮT PdfPreview legacy** (react-pdf + worker CDN unpkg) — lý do ở mục 6. i18n `createPost.quiz.*`(mới, en+vi) +`book.fileTooLarge`/`book.previewPagesHint`; tái dùng `createPost.book._`sẵn có. Sửa layout:`Input`/`Select`là`w-full`ở root nên`w-auto`không co được → dùng grid ở parent, áp cho cả`PollFields`(c-3). **Verify BE thật + BẰNG MẮT** (extension Chrome kết nối được lần này): 8 nhánh validate đo bằng API — free 200 · paid thiếu previewPages 400 · previewPages == tổng 400 · previewPages=1 200 · sai đuôi file 400 · quiz thiếu title 400 · quiz 1 option 400 ·`correctOptionIndex`ngoài khoảng 400 (**message có index câu sai**); SQL xác nhận`total_pages=3`, `is_free`, `preview_pages`, quiz JSON đúng `correctOptionIndex`. **End-to-end qua UI**: chọn PDF → chip tên tệp → nhập giá → ô `previewPages`hiện ra + nút Đăng khoá lại → điền quiz + chọn đáp án đúng bằng Radio → đăng → **DB có post BOOK + hàng t_books + quiz đúng đáp án đã chọn**; chọn`.txt`→ lỗi inline;`previewPages=3`→ banner 400 nguyên văn của BE, **nháp giữ nguyên**. Radio khớp specimen DS, light+dark đều đúng. **Trả nợ c-3**: 5 panel c-3 đã được xem tận mắt trong cùng route preview. Đã xoá route preview + 4 post test + file MinIO. |
| 2026-07-27 | P2.4d | Wiring + xoá legacy, posts chu kỳ 1.`/newsfeed`render`PostComposer`của`features/posts`; feed + card vẫn legacy (P2.5/P2.4′). **Seam `onPosted`**: domain `newsfeed`chưa tồn tại nên page invalidate key feed legacy, nhưng import hằng`NEWSFEED_QUERY_KEY`mới export từ`lib/hooks/use-posts.ts`thay vì lặp chuỗi — P2.5 xoá hook thì import gãy tiếng to (chi tiết + chỗ sẽ đổi: findings/posts.md). **Nhánh EVENT không xoá**: rút`create-post-form.tsx`thành`create-event-form.tsx`(card riêng dưới composer, cố ý xấu tới P2.4″d) — xoá trọn sẽ mất đường tạo post EVENT của cả app. Cầu tạm này gọi **hook + LocationPicker của`features/posts`**, nhờ vậy bản legacy chết ngay trong checkpoint này. **Đã xoá**: `create-post-form.tsx` `pdf-preview.tsx` `location-picker.tsx` `location-badge.tsx`; `useCreatePost`/`useCreateBookPost`/`useUpdatePost`/`useDeletePost`+`toCreatePostRequest`ở`lib/hooks/use-posts.ts`; 4 hàm write ở `lib/api/posts.ts`. i18n `createPost.event.contentPlaceholder`/`bridgeNote`(en+vi). **Verify thật trong trình duyệt**: đăng REGULAR →`POST /posts` 200 → **`GET /feed`refetch ngay sau** (network log) → banner "có thể phải qua kiểm duyệt", nháp reset, **feed đổi nội dung**; đăng lại y nguyên trong 60s → banner đỏ nguyên văn`Content violates community guidelines: [DUPLICATE_CONTENT]`, **nháp giữ nguyên**; EVENT qua cầu tạm → `POST /posts`200 +`GET /feed`, **DB có post EVENT id 111, `event_details`đủ title/description/start/end**. Console sạch trừ lỗi Twilio có sẵn (domain chat chưa làm). **Phát hiện: feed của BE có cache** — trả về bài đã xoá khỏi Postgres (ghi vào findings). 3 post test đã xoá khỏi DB. |
| 2026-07-25 | P2.4c-2 | posts UI chu kỳ 1 (tách 2/5):`LocationPicker`+`LocationBadge`, gắn vào `PostComposer`nên payload có`googlePlaceId`/`locationType`/`locationDetails`. Panel **inline** thay vì popover (DS không có spec floating-layer). Debounce 500ms + tối thiểu 3 ký tự trong component. Verify BE thật: gõ 16 ký tự → **đúng 1 request**, Gemini trả "Chợ Bến Thành…" PLACE → chọn → chip có link Maps của BE → đăng → **DB lưu đủ `google_place_id`+`location_details`** (legacy mất trắng, xem mục 6); query vô nghĩa → **400 có message** (không phải mảng rỗng — sửa lại ghi chú P2.4a); GPS bị chặn → thông báo đúng; dark remap; nút GPS bị wrap 2 dòng → sửa. Post test đã xoá. |
| 2026-07-25 | P2.4c-1 | posts UI chu kỳ 1 (tách 1/5): shared `Textarea`(**DS không có spec** — mirror Input) +`Select`(từ`Select.d.ts`+ specimen); feature`PostComposer`(REGULAR: content + visibility + submit). **Sửa fidelity`Input`**: specimen dùng `border-default`/pad 10px/label `text-primary`, bản cũ dùng `border-strong`/12px/`text-secondary`→ sửa cho 3 primitive cùng họ. Cross-domain refresh qua prop`onPosted`, không import key của newsfeed. Verify BE thật: create→200→clear + thông báo chờ kiểm duyệt, lỗi 400 → banner + **giữ nguyên nội dung nháp**, autoResize 42→84px, Select khớp specimen, dark remap; 2 post test đã xoá. |
| 2026-07-25 | P2.4a | posts data layer chu kỳ 1/3 (core): `features/posts/{types,api}`—`postsApi`(5) `locationApi`(1) `quizApi`(1) = **7/7 endpoint** của PostController+LocationController+QuizController. Nullability xác nhận từ Java: `googleMapsUrl`**nullable** (không có toạ độ), 3 field còn lại của resolve luôn có;`QuizResult`đủ 3 field.`LocationResolutionRequest`dựng thành union query \| toạ độ để sai tổ hợp chết lúc compile thay vì 400. Extraction test **chỉ`core/`**. Smoke test BE thật: resolve→shape khớp, thiếu cả 2 field→400, createPost BOOK→400 đúng message, create→update→delete→quiz submit (score 1/2, lệch số câu→400); post test đã xoá. Phát hiện: **domain write-only** — xem mục 6. |
| 2026-07-25 | P2.3 | `features/reputation`trọn gói (Tier C, 1 stop): types+api (1/1) +`useReputation`+ UI`RepScore` `ReputationCard` `MyReputationCard`, wire vào `/profile`. **Không hardcode ngưỡng lần 3** — `RepScore`nhận`levelName` từ API thay vì tự suy (deviation #11), thanh tiến trình chạy 0→`nextLevelMin`vì BE không gửi sàn cấp hiện tại. i18n`reputation._`. Verify BE thật ở 4 trạng thái score (0/15.8k/50k/404) + loading + dark. Extraction test sạch (thêm cạnh `@/features/security` qua barrel, hợp lệ §4). |
| 2026-07-25 | P2.2d | Wire 3 route friends → component feature (`/friends`giữ redirect). **Cắt`/friends/birthdays`** (deviation #7) + key i18n mồ côi. Xoá legacy hết consumer: `useInfiniteFriends` `useSentRequests` `useCancelFriendRequest`, `friendshipApi.getSentRequests`/`cancelRequest`, type `SentFriendRequest(Wire)`, **cả `src/lib/mock/`**. Giữ 6 hook legacy còn consumer ngoài domain — xem mục 6. Verify BE thật: 3 route render qua feature, `/friends`→`/friends/all`(13 bạn), birthdays→404, nav 3 link + badge 1, dashboard/chats/picker bạn bè không hồi quy. |
| 2026-07-25 | P2.2c-3 | UI 1 màn (tách 3/3):`FriendSuggestions` (`/friends/suggestions`) — gợi ý + số bạn chung + gửi lời mời, chéo `sent` để khoá nút đã gửi. Cắt nút "Bỏ qua" (DS deviation #9). Verify BE thật: 3 gợi ý xếp theo bạn chung 2/1/1, gửi→`POST /requests/9009`200→invalidate sent+suggestions→nút thành "Đã gửi lời mời" disabled; nút secondary 28px/13px, icon 16px, dark remap. Suggestions rỗng do graph thiếu FoF — xem mục 6. **UI friendships xong 3/3 màn.** |
| 2026-07-25 | P2.2c-2 | UI 2 màn (tách 2/3):`FriendsList` (`/friends/all`) + `FriendRequests` (`/friends/requests`, tab nhận/gửi), primitive `Tabs`. Sửa lỗi hạ tầng `cn()`nuốt class cỡ chữ DS (xem mục 6). Verify BE thật: 13 bạn (cursor 3 trang), accept→pending 2→1 + friends 12→13, reject→empty state, cancel→rời tab gửi; tab xanh 2px + pill mono, arrow-key nav, dark remap. Phát hiện Neo4j rỗng — xem mục 6. |
| 2026-07-24 | P2.2c-1 | UI primitives (tách 1/3): shared`Badge` `Skeleton` `EmptyState`(từ DS .d.ts+specimen), feature`FriendListItem`. Thêm keyframes DS (nx-shimmer/blink/enter/fade) vào globals. Verify preview: khớp DS specimen, animation chạy, cursor amber, light/dark. birthdays sẽ **CẮT** ở 2d (dữ liệu bịa). |
| 2026-07-27 | P2.4'a | Data layer chu kỳ 2 posts (engagement): `types/comment.ts`+`types/reaction.ts`+`api/comment.ts` (`commentsApi`, 4/4) + `api/reaction.ts` (`reactionsApi`, 3/3) = **7/7**, posts đạt 14/22. Mở rộng barrel sẵn có, không đẻ module. Nullability xác nhận từ Java + đo API thật: `{"reactionType":null}`khi chưa thả;`updatedAt`sinh ngay lúc INSERT. 5 phát hiện vào`findings/posts.md`(thread 2 tầng · cascade xoá reply ở DB · không có`isEdited`· không có tổng reaction · un-react khi chưa thả là 404). Drift check rỗng. |
| 2026-07-27 | P2.4'b | State layer chu kỳ 2 posts:`hooks/use-comment.ts` (`useComments`+ 3 mutation, tự invalidate`postKeys.comments`— **dùng lại key chu kỳ 1**, không đặt key mới) +`hooks/use-reaction.ts` (`useMyReaction`+ upsert/remove **optimistic có rollback**) +`postKeys.myReaction`. `groupComments`gom thread 2 tầng ở tầng state. Sửa type P2.4′a: nullable →`\| null`chứ không`?:`(Jackson`ALWAYS`, key có mặt giá trị null). Đo thật 5 hành vi (reply-to-reply 400 · blank 400 · cascade xoá reply · un-react 404 · upsert đổi emoji). Dev DB đã dọn. |
| 2026-07-27 | P2.4'c-1 | UI chu kỳ 2 (tách 1/5): shared `DeveloperIdentity`+`DeveloperMeta`; `PostCard`vỏ + hàng danh tính + content + location + hashtag + đếm bình luận, với 3 slot`body`/`actions`/`menu`. Card **nhận props đã tách, không nhận `FeedPostDataDto`** (DTO đó thuộc package newsfeed → nhận vào là ngược chiều §4). Tách c-1 thành c-1 + c-1′ vì payload feed có 8 khối details, vượt trần 5 component. 4 DS deviation mới (#14 shareCount chết · #15 feed không echo `images`· #16 RepScore thành slot · #17 không dựng ExpertiseTag). **NỢ: chưa verify bằng mắt** — extension Chrome không nối; đã kiểm bằng SSR markup 14/14 + không rò i18n. |
| 2026-07-27 | P2.4'c-1'a | UI chu kỳ 2 (tách 2/5): 4 khối body đổ vào slot`body`của`PostCard`—`CodeSnippetBody`(mono, **không highlight**:`language`là String tự do, DS không spec màu syntax),`ArticleBody`+`LinkBody`(ảnh là URL tác giả tự dán →`onError`ẩn hẳn; LinkBody **chặn URL không parse được** để không thành link tương đối, host luôn hiện vì BE không unfurl),`QnaBody`(một pill;`bountyPoints`cắt theo #12,`acceptedAnswerId`để c-4). Cả 4 render`null`khi rỗng vì BE không validate 4 loại này. **NỢ verify bằng mắt cộng dồn c-1 + c-1'a** — extension vẫn không nối; SSR markup 19/19, không rò i18n. |
| 2026-07-27 | P2.4'c-1'b | UI chu kỳ 2 (tách 3/5): 3 khối body còn lại —`PollBody`(lựa chọn **trơ**: không có endpoint bỏ phiếu,`votesCount`vĩnh viễn 0 nên không render),`BookBody`(trình bày thuần + slot`actions`; mua/đọc thuộc **bookstore** P2.10, gọi sang là phạm §4), `EventBody`(không có RSVP vì **chu kỳ 3**, không phải thiếu endpoint;`maxAttendees`là trần chứ không phải số đã đăng ký). **Đủ 7/7 body.** Phát hiện **lỗi BE**:`coverImageUrl`là presigned URL 24h nhưng bị ghi vào`t*books`và echo mãi → ảnh bìa sách chết sau 1 ngày. Guard URL dùng chung với LinkBody chặn`javascript:`. SSR markup 24/24 (2 MISS ban đầu là lỗi của chính biểu thức kiểm, đã truy lại). **NỢ verify bằng mắt vẫn cộng dồn** c-1 + c-1'a + c-1'b. |
| 2026-07-27 | P2.4'c-2 | UI chu kỳ 2 (tách 4/5): `ReactionBar`vào slot`actions`của`PostCard`— **5 toggle inline** (DS không có spec popover nên không dựng tray emoji nổi), **icon lucide đơn sắc** không emoji màu, active = viền +`aria-pressed`chứ không chỉ màu. **Gate 404**:`remove`chỉ bắn khi đúng reaction đang active, mọi thứ khác là upsert. Tổng reaction KHÔNG cộng tại chỗ — đến từ payload feed,`onChanged`để caller tự refetch. Đính chính comment sai ở`event-body.tsx` (`TranslateFn`nhận`string`, lý do viết key tường minh là greppability chứ không phải type). SSR 6/6. **NỢ: hành vi tương tác chưa chạy thật** — extension không nối và project **không có test runner**, không tự thêm. |
| 2026-07-27 | P2.4'c-v | **Checkpoint trả nợ verify** (user yêu cầu trước khi làm c-3). Extension Chrome nối lại được → **không thêm test runner**, đi đường trình duyệt (rẻ hơn, lại kiểm được diện mạo). Đăng nhập seed user qua form thật, verify c-1 + c-1'a + c-1'b + c-2: **hành vi `ReactionBar`4/4** (optimistic bật trước khi server trả · LIKE→LOVE là upsert · bấm lại là remove, **không 404 nào**), **diện mạo + dark mode** cả 9 card / 7 body, **so với specimen DS đã render** (khác biệt đều là deviation đã ghi; thêm #18). Bắt được **2 lỗi thật mà SSR không thấy**: format ngày sai locale (UI tiếng Việt hiện en-US) và **hydration mismatch ở`EventBody`**. Sửa chung ở `features/posts/lib/format.ts`— truyền locale tường minh, bỏ giây; áp cho EventBody/PollBody/PostCard/BookBody. Console sạch. Dev DB sạch. |
| 2026-07-27 | P2.4'c-3 | UI chu kỳ 2 (tách 5/5):`CommentThread`+`CommentItem`+`CommentComposer`— thread 2 tầng, **"Trả lời" chỉ trên comment gốc** (BE chặn reply-của-reply), xoá **xác nhận 2 bước tại chỗ có nói số reply sẽ mất theo cascade**, badge "đã sửa" suy từ`updatedAt !== createdAt`. Đổi tên type `CommentThread`→`CommentWithReplies`(trùng component); nhóm i18n mới là`post.comments.*`vì`post.comment`còn được card legacy dùng tới P2.4'd. **Verify bằng trình duyệt trên BE thật**: tạo qua UI (Ctrl+Enter) · đúng 1 nút Trả lời · cảnh báo "và 1 câu trả lời" · xoá gốc → DB 0 dòng + UI về empty (chứng minh refetch-không-splice) · sửa`PUT 200`rồi`GET`thread. **Bắt được lỗi thật: ô nhập không tự xoá sau khi gửi** → sửa bằng đổi`key`sau write thành công (giữ nháp khi lỗi), không dùng`useEffect`+setState (bị `react-hooks/set-state-in-effect`chặn, rule đúng). Dev DB sạch. |
| 2026-07-27 | P2.4'c-4 | UI chu kỳ 2 xong (bước cuối):`QuizTaker`+`PostEditor`+`PostMenu`, và nút chọn đáp án gắn vào `CommentItem`/`CommentThread`— trả nốt nợ c-5 của chu kỳ 1. Verify bằng trình duyệt trên BE thật, cả 4 endpoint: quiz chấm đúng (đánh dấu đáp án đúng + lựa chọn sai, khoá option sau khi nộp) · accept`PATCH 200`và lưu`acceptedAnswerId`· sửa bài`PUT 200`· xoá bài + comment cascade → DB sạch. **3 lỗi thật bắt được**: (1)`acceptAnswer`không bao giờ set`isResolved`→`QnaBody`phải đọc`acceptedAnswerId`, nếu không mọi câu đã có đáp án vẫn báo "chưa có"; (2) chọn đáp án là một lần, nút phải biến khỏi **cả thread** chứ không chỉ comment được chọn; (3) `current`thiếu field của`PostEditor`**đã xoá thật**`quiz*details`+`acceptedAnswerId`→ siết`PostEditorState`bắt buộc đủ 14 key ở tầng type, sửa lại với state đầy đủ thì dữ liệu sống sót. **Sự cố môi trường**: seed user bị moderation **tự ban 7 ngày** (Gemini gắn nhầm HATE_SPEECH cho nội dung test), ban chặn cả`/auth/login`— đã gỡ + xoá violation, ghi cách xử lý vào`session-constants`. |
| 2026-07-27 | P2.4'd | **Wiring + xoá legacy chu kỳ 2.** `/newsfeed`render`FeedPost`— adapter map`FeedPostDataDto`→ props`PostCard`+ 7 khối body +`ReactionBar`+`CommentThread`(chỉ mount khi người dùng mở: đo được **đúng 1** request`/comments`) + `PostMenu`/`PostEditor`/`QuizTaker`. Xoá: `post-card.tsx`, `event-post-details.tsx`, `book-post-summary.tsx`, **cả `lib/api/posts.ts`**, 7 hook comment/reaction trong `use-posts.ts`(còn đúng`useNewsfeed`+`NEWSFEED_QUERY_KEY`), 7 type trong `lib/types`(gồm`SessionComment`), 4 key i18n. Hai **cầu tạm mới**: `feed-post.tsx`(→ P2.5) và`book-post-actions.tsx`(→ P2.10 — xoá trọn`book-post-summary`sẽ mất đường mua/tải/xem trước/đánh giá sách duy nhất của app, Guardrail C). Sửa`FeedPostData`legacy: thiếu **8 field BE vẫn luôn gửi**, không khai thì TS báo không tồn tại. **Phát hiện lỗi BE chi phối cả checkpoint**:`fanOutPost`**không copy 6 khối details** → 5/7 body + quiz không bao giờ render từ feed, và sửa bài của 5 loại đó sẽ **xoá khối định nghĩa chúng** → thêm`PostMenu.canEdit`, tắt nút Sửa cho CODE_SNIPPET/ARTICLE/QNA/POLL/LINK. Thêm 2 lỗi BE: `likeCount`/`commentCount` **vĩnh viễn 0** (`updatePostCache`không có caller nào) → cắt cả hai số (deviation #19);`acceptedAnswerId`không đọc lại được → nút chọn đáp án không rút, bấm lần hai 400 im lặng →`CommentThread`nhớ trong phiên, prop vẫn ưu tiên. Thêm cảnh báo`post.edit.pendingReview` vì **sửa bài đẩy bài qua kiểm duyệt lại và bài biến khỏi feed** ~1 phút. Verify bằng trình duyệt trên BE thật: bình luận · trả lời (`parentId`đúng) · xoá gốc → cascade DB 0 dòng · reaction → DB`LIKE`· chọn đáp án →`acceptedAnswerId`lưu + nút rút khỏi cả thread · sửa bài → nội dung đổi mà **địa điểm còn nguyên** · xoá bài → mất khỏi DB lẫn feed · bài người khác **không có menu** · light + dark. Console sạch (chỉ lỗi Twilio có sẵn của domain chat). Dữ liệu test dựng bằng đường **insert + admin review**, không chạm Gemini → 0 violation, seed user không bị ban. Dev DB trả về nguyên trạng (2 post seed, 0 comment/reaction/book), cache feed đã dọn sạch bài ma. |
| 2026-07-27 | P2.4"a | Data layer chu kỳ 3 posts (events):`types/event.ts`(5 type derive từ`schema.gen.ts`) + `api/event.ts` (`eventsApi`, **7 hàm / 8 endpoint** — `google/callback`là redirect của Google về BE,`permitAll`, FE không gọi; đúng 1 trong 2 endpoint N/A của ledger). Mở rộng barrel sẵn có, **không đẻ `features/events/`** (path `/events`là ngoại lệ, package`com.socialapp.posts`mới là luật §4). **posts đạt 22/22 endpoint ở tầng types+api.** Đo hợp đồng thật trên API (event id 141, xoá sau khi đo): upsert RSVP giữ 1 dòng ·`count`chỉ đếm GOING ·`maxAttendees`chỉ chặn GOING (400 'Event is full') · 400 'Post is not an event' · 400 khi thiếu/sai`status`· 404 'Google Calendar not connected' · ICS 200 có Bearer và **401 không có**. 6 phát hiện vào findings, gồm **lỗi legacy**`getExportIcsUrl`dựng URL trần cho`<a href>`nên luôn 401, **lỗi bảo mật BE** OAuth`state`= user id trần trên endpoint`permitAll`(gắn lịch vào tài khoản người khác được),`/attendees` trả JPA entity **chỉ có userId** nên không hiện được danh tính, và **Google Calendar không cấu hình ở local** (`client_id`rỗng) nên luồng nối lịch không verify end-to-end được ở bước d. Drift check rỗng. |
| 2026-07-27 | P2.4"b | State layer chu kỳ 3 posts (events):`hooks/use-event.ts` — 4 query (`useAttendees` `useAttendeeCount` `useCalendarStatus` `useGoogleAuthUrl`) + 2 mutation (`useRsvp` `useAddToGoogleCalendar`) + `findMyRsvp`. Key mở rộng `postKeys`sẵn có, **không đặt namespace mới**: hai key theo event nằm dưới`post(postId)` vì event \_là* post; hai key Google Calendar theo **user** nên đứng ngoài id (nhiều card chia nhau 1 request). **RSVP CỐ Ý KHÔNG optimistic** — khác reaction ở chỗ server **từ chối được** (400 "Event is full"), mà count phía client là số cache do RSVP của người khác làm lệch → optimistic sẽ hay hiện "đang tham gia" cho sự kiện vừa từ chối mình rồi nháy ngược lại. `useRsvp` invalidate **cả hai** read vì đổi GOING→INTERESTED làm count đổi mà độ dài list thì không. **`findMyRsvp` là suy, không phải fetch** (không có `/rsvp/me`): phân biệt `null` = chưa từng trả lời với `NOT_GOING` = đã trả lời không, vì **không có đường huỷ RSVP** nên ai đã trả lời thì luôn còn dòng. `useGoogleAuthUrl(enabled)` bắt buộc truyền `enabled` (không default) + `staleTime: Infinity`. Extraction test lại: cạnh ngoài biên của `features/posts` vẫn chỉ có core/shared/feature-barrel + `@/lib/i18n` (đã ghi nợ từ P2.2c-2), không có Redux. **posts đạt 22/22 ở types+api+hooks.** |
| 2026-07-27 | P2.4"c | UI chu kỳ 3 posts (events), **2 component, không cần tách**: `EventRsvpBar` + `EventCalendarActions`, cả hai đổ vào slot `actions` của `EventBody` (slot mở sẵn từ c-1′b). RSVP là **3 toggle không có undo** — BE không có endpoint rút RSVP nên bấm lại lựa chọn đang active là **no-op** chứ không phải bỏ chọn (`NOT_GOING` là câu trả lời khác, không phải xoá). **Không hiện được ai tham gia, chỉ hiện bao nhiêu** (`/attendees` chỉ trả `userId`); vẫn phải fetch list vì đó là đường duy nhất biết mình đã trả lời gì. Rào "đã đầy" ở FE là **phép lịch sự, không phải bản sao luật**: server vẫn là nơi quyết, nhánh 400 vẫn sống. Tầng state nhận thêm `useExportIcs` — **mutation dù là GET**, vì nó chạy theo yêu cầu và kết quả là một file chứ không phải state đáng cache. Verify bằng route preview tạm (đã xoá) trên BE thật: GOING → count 0→1 · bấm lại nút active → **0 request thêm** · đổi INTERESTED → count về **0** (count chỉ đếm GOING — đúng lý do phải invalidate cả hai key) · rào đầy: GOING disabled + caption · bỏ rào để server từ chối → hiện **"Event is full"** và nút **không bao giờ nhảy sang pressed** (bằng chứng cho quyết định không optimistic) · tải .ics: blob 243 byte VCALENDAR thật, tên file lấy từ tiêu đề · bấm "Kết nối" → refetch + redirect thật tới Google, Google báo `Missing required parameter: client_id` (đúng lỗ cấu hình local đã ghi ở ″a). Light + dark đúng token, console sạch. **Bắt được lỗi của chính mình khi đo**: hàm đặt tên file chứa **byte NUL + 0x1F thật** trong character class (công cụ ghi file biến escape thành byte) → class biến thành range control, file bị git coi là **binary**, code nói một đằng làm một nẻo; lộ ra vì tên file tải về không khớp với kết quả chạy lại chính regex đó bằng node. Đã vá byte, bỏ escape khỏi source, quét lại toàn bộ file đã đổi. Dev DB + cache trả nguyên trạng. |
| 2026-07-27 | P2.4"d | **Wiring + xoá legacy chu kỳ 3 — posts ĐÓNG DOMAIN (22/22, cả 4 cột).** 1 màn + 1 component mới (`EventFields`), không cần tách. `PostComposer` nhận loại **EVENT** → làm đủ **8/8 loại**, và đó là thứ giết được cầu tạm `create-event-form.tsx` cùng `event-datetime-picker.tsx`/`event-location-input.tsx`/`event-datepicker.css`; `feed-post.tsx` gắn `EventRsvpBar` + `EventCalendarActions` vào slot `actions` của `EventBody`. **Dựng lại ô nhập bằng `datetime-local` native** thay `react-datepicker` (gỡ luôn khỏi `package.json`, 0 consumer) nhưng **giữ nút tắt +1/+2/+3 giờ** — đó mới là phần picker cũ thực sự đáng giá. `EventDetails.location` thành ô text thường: composer đã có `LocationPicker` cho địa điểm có cấu trúc, bắt tác giả chờ Gemini 5–9s để điền một nhãn là trao đổi tệ. Xoá thêm: `lib/api/events.ts`, `lib/hooks/use-events.ts`, `lib/api/location.ts`, `lib/hooks/use-location.ts` (cả 4 file), 14 type mồ côi trong `lib/types` (mặt ghi của posts + Events + `PostLocation`/`LocationResolutionResponse`/`EventDetails`), 6 nhóm key i18n của cầu tạm. **`src/lib/api/` và `src/lib/hooks/` không còn file nào của posts.** Verify trên BE thật: tạo post EVENT **qua composer mới** → DB lưu `event_details` đủ, `start/end` ISO đúng múi giờ (18:00 → 20:00 local, không lệch UTC) · nút tắt +2h tính đúng · RSVP **ngay trên card feed** → `aria-pressed` bật, count 0→1, DB `GOING` · cầu tạm biến mất khỏi trang · light + dark, console sạch. **Hai lỗi bắt được khi bấm thật**: (1) rò key i18n `createPost.type.EVENT`/`contentPlaceholder.EVENT` — nhóm key cũ không có EVENT vì cầu tạm dùng chuỗi riêng; (2) **mọi sự kiện không giới hạn chỗ đều hiện "đã đủ người" và khoá nút GOING** — rào `isFull` viết `maxAttendees !== undefined` trong khi payload trả **`null`** (Jackson `ALWAYS`), mà `0 >= null` là `true` trong JS. Đúng cái bẫy null-vs-undefined đã ghi ở P2.4′a, lần này rơi vào từ phía consumer; đã sửa thành `!= null` và nới type prop. Dev DB + cache trả nguyên trạng, 0 violation. |
| 2026-07-27 | P2.5 | **Domain `newsfeed` (tier C, 1 checkpoint cả 4 lớp).** Dựng `features/newsfeed/`: `types/feed.ts` + `api/feed.ts` (1/1 endpoint) + `hooks` (`newsfeedKeys`, `useNewsfeed` infinite) + `components` (`Newsfeed` dựng lại bằng `shared/components`, `FeedPost` chuyển từ legacy sang). **Type mã hoá đúng sự thật null**: mọi field optional thành `| null` chứ không `?:`, vì Jackson `ALWAYS` gửi key kèm null — chính khác biệt đã gây 2 lỗi thật (P2.4′a xếp nhầm comment gốc thành reply, P2.4″d khoá nút RSVP vì `0 >= null`). Type mới **bắt được ngay một chỗ ép kiểu ngầm** ở `toEditorState` (`content` nullable). **Đóng seam P2.4d**: `NEWSFEED_QUERY_KEY` chết, page dùng `newsfeedKeys.feed()`. Xoá legacy: `lib/api/newsfeed.ts`, `lib/hooks/use-posts.ts` (file cuối cùng của cụm posts/newsfeed), `FeedPostData`/`FeedResponse`/`FeedBookSummary` trong `lib/types`, `components/posts/newsfeed.tsx` + `feed-post.tsx`. **Ranh giới**: cầu tạm bookstore **không** được import vào feature — page truyền xuống qua render prop `renderBookActions`, nên extraction test của `features/newsfeed` sạch (chỉ core/shared/feature-barrel + `@/lib/i18n` đã ghi nợ). Verify trên BE thật với 13 bài: 10 card trang 1 → cuộn → **13 card / 2 request** (infinite scroll thật, `hasMore` đúng) · nhánh lỗi: ép request feed hỏng bằng cách bấm cảm xúc → hiện empty-state lỗi + nút "Thử lại", bấm lại → **hồi phục đủ 13 card** · empty state thật (xoá cache Redis) hiện glyph `>_` của DS thay emoji legacy · console sạch. **Ghi chú môi trường đáng nhớ**: tab Chrome bị `document.visibilityState = 'hidden'` thì **IntersectionObserver không chạy**, nên "cuộn mà không nạp thêm" lúc đầu là do tab ẩn chứ không phải lỗi code — kiểm `visibilityState` trước khi nghi ngờ observer. Dev DB + cache trả nguyên trạng. |
| 2026-07-27 | P2.6ab | **Data + state layer `notifications` (tier B, stop 1/2).** Dựng `features/notifications/`: `types/notification.ts` + `types/preference.ts` + `api/notification.ts` (4 hàm) + `api/preference.ts` (2 hàm) = **6/6 endpoint** + `hooks` (`notificationKeys`, `useNotifications` infinite, `useUnreadNotificationCount` poll 30s, 2 mutation đọc, 2 hook preference) + 1 barrel. **KHÔNG dựng UI** và **KHÔNG xoá legacy** — domain này là ca "6/6 endpoint, 0 UI" nên không có consumer nào để rewire; xoá `lib/api/notifications.ts` + `lib/hooks/use-notifications.ts` trước khi có consumer là mass-delete, trái Guardrail B. **Kiểm BE trước khi thiết kế thay vì suy từ tên endpoint**: grep `WebSocketConfig`/`@EnableWebSocket*`/`SseEmitter`/`text/event-stream` toàn bộ `src/main/java` → **không có realtime nào**, nên polling là đường duy nhất chứ không phải lựa chọn lười. **Đóng dòng lệch shape P0.3**: `NotificationPreference` legacy khai `id: number` và thiếu `updatedAt`; entity thật là `@Id private Integer userId` + `@UpdateTimestamp updatedAt`, verify trên wire đúng như vậy. Smoke test **6/6 endpoint** trên BE thật với seed user 9001 (3 notification `FRIEND_REQUEST` có sẵn từ luồng friendships): `?page=1` → `"number":0` và `?page=0` → **400** (request 1-based, response 0-based → cursor `+2` chứ không `+1`) · `POST /999999/read` → **200** (no-op im lặng, không 404/403 — lý do hook invalidate thay vì optimistic) · `PUT {"emailEnabled":false}` giữ nguyên `pushEnabled` (partial update dù là `PUT`) · `read-all` → count 3→0. **Phát hiện đáng kể**: spec khai `Page.sort` là `SortObject[]` nhưng wire trả **object** → `NotificationPage` dùng `Pick` 7 field (Pick sai key là lỗi compile, `Omit` thì im lặng) và bỏ hẳn `pageable`/`sort` · `emailFrequency` là **setting chết** (không `@Scheduled` nào đọc, `NONE` vẫn gửi mail ngay) → phải công bố cắt-hay-dán-nhãn ở đầu cd · `GET /preferences` **có tác dụng phụ ghi DB** (tạo hàng mặc định, không bao giờ 404) · 4/11 `NotificationType` không có nơi phát · OneSignal `app-id` rỗng ở local nên nhánh push **không verify end-to-end được**. **Không dựng `Page<T>` dùng chung** — mỗi feature derive envelope riêng từ schema của mình, gộp lại là dựng lại bucket xuyên domain §4 cấm. Type đặt tên `AppNotification` vì `Notification` là global của DOM lib. Drift check rỗng. Dev DB trả nguyên trạng (3 notification về `is_read=false`, `emailEnabled` về true qua chính API). |

| 2026-07-27 | P2.6cd | **UI + wiring `notifications` — domain ĐÓNG (6/6, cả 4 cột).** 1 màn + 4 component mới (shared `Switch`, `NotificationItem`, `NotificationList`, `NotificationPreferences`), dưới trần 2 màn / 5 component. **Hai quyết định công bố ở ĐẦU bước**, không giữa chừng: (1) surface là **route mới `/notifications`** chứ không rewire trang nào — domain 0 UI nên không trang nào giữ chức năng của nó, Guardrail C không có gì để đối chiếu; **chuông + badge KHÔNG dựng**, thuộc app shell P3.4, `unread-count` lấy consumer ở header của chính trang này nên không endpoint nào phải chờ. (2) **`emailFrequency` CẮT** (DS deviation #20) vì không `@Scheduled` nào đọc nó và `NONE` vẫn gửi mail ngay — nhãn "chưa có hiệu lực" vẫn là mời người dùng lưu một giá trị nói dối; `pushEnabled`/`emailEnabled` giữ vì được đọc thật. **DS có `Switch`** (suýt kết luận nhầm là không, do output `ls` bị cắt): doctrine `.d.ts` nói thẳng "On/off toggle for immediate-effect settings, use Checkbox inside forms" → panel lưu-ngay-khi-gạt, khớp `PUT` partial của BE (một switch gửi đúng một field). Hình học đọc từ specimen đã render: track 32×18, knob 14×14 dịch 14px, off = `#c8cdd3` = `--nx-border-strong` (nên dark mode tự đúng), 200ms = `--nx-duration-base`. **Xoá legacy**: `lib/api/notifications.ts` + `lib/hooks/use-notifications.ts` (cả 2 file) + 7 type trong `lib/types` + 2 dòng barrel; **giữ `Page<T>`** vì moderation/social còn dùng. **Di chuyển đã hẹn trước**: `features/posts/lib/format.ts` → `shared/lib/format.ts` và `useRelativeTime` từ trong `post-card.tsx` → shared (chính hai file đó viết sẵn điều kiện "promote khi domain thứ hai cần"); i18n `post.justNow/*Ago` → `time.*`, kéo theo 2 file legacy chat/trending phải sửa key. **Verify bằng trình duyệt trên BE thật**: 3 notification thật (FRIEND_REQUEST từ luồng friendships) · bấm 1 dòng → mark-read, đếm 3→2, dòng hết đậm/hết chấm/đổi icon xám, và **điều hướng sang `/friends/requests`** · "Đánh dấu tất cả đã đọc" → 0 chưa đọc + nút tự disable · gạt mute POST_LIKED → DB `muted_types=["POST_LIKED"]` mà `push_enabled`/`email_enabled` **giữ nguyên** (bằng chứng partial update qua UI thật) · light + dark · console sạch (chỉ lỗi Twilio có sẵn của chat). **Bắt được lỗi thật của chính mình khi verify, bằng `data-debug` gắn tạm vào DOM**: nhánh `isLoading ? skeleton : isError ? error : empty` **bỏ sót** trạng thái RQ `status:pending` + `fetchStatus:paused` (cả hai cờ đều false) → **list đang hỏng mà UI báo "Chưa có thông báo nào"**, tức app tự khẳng định một điều chỉ server mới được phép khẳng định. Sửa thành: skeleton khi `isPending && fetchStatus===fetching`, error khi `status !== success`, empty **chỉ khi** `status === success`. **`features/newsfeed` có đúng cùng lỗ** — không sửa ở đây (domain khác), đã ghi nợ vào `findings/newsfeed.md` cho P3.1. **Một việc CHƯA giải thích được, ghi rõ thay vì đoán**: từ trạng thái lỗi, nút "Thử lại" **không phát request nào** (đo 2 lần, 2 kiểu lỗi khác nhau, network rỗng sau khi clear); khớp với query bị RQ park ở `paused`, nhưng `navigator.onLine` là `true` và CORS trên 400/500 có đủ header — chưa kết luận được là lỗi FE hay môi trường automation. App **hồi phục khi reload**. Cách kiểm tiếp + phương án `networkMode` ở `findings/notifications.md` §14. Dev DB trả nguyên trạng (3 notification về unread, `muted_types` về `[]`).|

| 2026-07-27 | P2.7a | **Data + state layer `chat` (tách 1/4).** Domain này **không còn là tier C 1-checkpoint** — 1 endpoint nhưng ~1830 dòng UI trên **2 surface** (`/chats` + cửa sổ nổi trong app shell), vượt xa trần 2 màn/5 component; công bố tách **P2.7a / c-1 / c-2 / d** ở đầu domain trước khi viết code. Cửa sổ nổi **nằm trong P2.7 chứ không đẩy sang P3.4** (khác chuông thông báo): nó đang tồn tại thật, xoá bản Twilio mà không dựng bản thay là hồi quy (Guardrail C). **BE đã sửa xong R1–R5 do checkpoint trước yêu cầu** — verify lại từng cái: token 200, header `{"alg":"HS256"}` (R2 đúng là lỗi thật, jjwt tự chọn HS512 với secret 64 ký tự), có `apiKey`, có `iat`/`exp`+`expiresAt`, và `upsertUser` chạy thật (đo trên UI: hiện **"Backend Vũ Thảo"** chứ không phải `9004`). R6 = phương án (a), không endpoint mới → ledger vẫn 101; drift chỉ 3 dòng `ChatTokenResponse`, đúng như dự kiến. Dựng `features/chat/`: `types` + `api` (1/1) + `hooks` (`ChatClientProvider`/`useChatClient`, `useConversations`, `useConversation`). **4 quyết định kiến trúc**: dùng `stream-chat` client thuần **không** `stream-chat-react` (bộ UI dựng sẵn trái Constraint #1); **không component nào import `stream-chat`** — hook map sang `ChatConversation`/`ChatMessage` của feature (dự án vừa mất một checkpoint vì FE dựng trên Twilio còn BE cấp Stream, UI bám shape SDK là UI phải viết lại lần sau); client nằm trong **React context**, không React Query (nó là websocket có store SDK tự mutate, không phải snapshot bất biến) không Redux, **một connection cho cả app**; `connectUser` nhận **hàm** cấp token chứ không phải chuỗi, nếu không hết 24h là rớt socket không có đường về. **Không có query key namespace** — không query nào để đặt key, ghi rõ trong `hooks/index.ts` để người sau không tưởng là sót. 503 tách thành trạng thái riêng `unconfigured` thay vì gộp vào `error` (retry không bao giờ cứu được lỗi thiếu cấu hình). **Verify thật bằng route preview tạm** (đã xoá): `connected` userId 9001 · tạo hội thoại → channel distinct-by-members `!members-...` · tên đối phương đúng · gửi tin → tin hiện qua echo `message.new` (không cần optimistic) · list tự cập nhật 0→1 kèm preview + `lastMessageAt` · đóng/mở lại đúng. Console **0 lỗi từ lớp Stream** (chỉ còn tiếng ồn Twilio legacy, chết ở P2.7d). **Phát hiện chặn tính năng — R8 mới cho BE**: Stream **từ chối tạo channel với user chưa tồn tại**, mà `upsertUser` chỉ chạy cho người đang xin token → **không nhắn được cho ai chưa từng mở chat**, tức gần như cả danh sách bạn bè. Xác nhận đúng nguyên nhân bằng cách cho 9004 đăng nhập một lần (bật tạm `email_verified`, gọi `/chat/token`, tắt lại) → tạo channel thành công ngay. 3 phương án sửa ở `findings/chat.md` R8, **cần chốt trước P2.7c-1**. **Hai bẫy đã vấp**: Strict Mode gọi effect 2 lần làm `connectUser` chạy chồng ("connectUser called twice", chỉ xuất hiện ở dev, trông y hệt lỗi kết nối thật) → chặn bằng ref; `react-hooks/set-state-in-effect` chặn kiểu reset-state-đầu-effect ở **cả hai** hook → sửa bằng **suy giá trị** (gắn thẻ state theo id hội thoại rồi che dữ liệu cũ), tiện thể diệt một glitch thật là nháy tin nhắn của hội thoại trước dưới header mới khi đổi hội thoại. `channel.data.name` phải `declare module` vì v9 để custom field trong `CustomChannelData` rỗng. Fixture giữ lại có chủ đích: 1 channel + 2 tin giữa 9001/9004 trên Stream cho P2.7c-1 dựng UI.|

| 2026-07-28 | P2.7c-1 | **UI màn `/chats` (tách 2/4).** 5 component mới, đúng trần: `ConversationRow`, `ConversationSidebar`, `MessageBubble`, `MessageComposer`, `ConversationView` (+`ConversationEmpty` cùng file); `NewChatPanel` và `ConnectionNote` để **cục bộ không export** vì chưa có caller thứ hai định hình props. **Ranh giới chốt: `features/chat` import `features/friendships` qua barrel** — mở hội thoại mới = chọn một người bạn nên phụ thuộc là có thật, và **BE vẽ đúng ranh giới đó** (`com.socialapp.chat` gọi `FriendshipService.getFriendIds` từ R8) nên FE mirror lại thì hai đồ thị phụ thuộc trùng nhau; luồn qua prop sẽ phải nối ở **hai** điểm mount và giấu một phụ thuộc thật sau cái prop — thứ §4 cảnh báo mạnh hơn cả việc import. **DS không có specimen nào cho chat** (chỉ actions/display/forms/navigation/overlays) → `MessageBubble` tự dựng từ token, ghi **deviation #21** để người sau biết không phải bỏ sót mẫu. **Lỗi thật bắt được khi nhìn màn hình**: badge chưa đọc **không tự tắt khi tin tới lúc hội thoại đang mở** — `markRead()` chỉ chạy lúc mở, nên sidebar báo "1 chưa đọc" cho đúng cuộc trò chuyện người dùng đang nhìn; sửa bằng listener `message.new` thứ hai gọi `markRead()`, **bỏ qua tin của chính mình** (echo lệnh gửi không cần xác nhận, và làm vậy sẽ nhân đôi lưu lượng ghi). Đo lại cả hai chiều sau khi sửa. **Verify trên BE + Stream thật** bằng route preview tạm (đã xoá): 2 hội thoại hiện tên thật + preview + thời gian tương đối · gửi tin tiếng Việt có dấu, ô nhập tự xoá · **bơm tin đến bằng chính user token của 9004** (login 9004 → `/chat/token` → POST thẳng vào API Stream với `Stream-Auth-Type: jwt`) → bong bóng trái + avatar + bo góc theo run hiện **realtime không cần reload**, sidebar cập nhật preview · badge: gửi khi ở trang khác → hiện `1`, mở ra → tự tắt; gửi khi đang mở → không hiện · light + dark. **Bẫy encoding**: chuỗi tiếng Việt qua `curl -d` trên Git Bash hỏng UTF-8 → Stream trả 400 `message.text is not valid utf8 text`; ghi body ra file bằng node rồi `--data-binary @file`. **Chi tiết giữ lại có lý do**: `isComposing` chặn Enter của bộ gõ telex/VNI (không chặn thì Enter chốt từ sẽ gửi mất một từ dở — app tiếng Việt chắc chắn gặp); ô nhập **chỉ xoá khi gửi thành công** và xoá trong handler chứ không trong effect; tự cuộn đáy phụ thuộc `messages.length` **không phải** `messages` (mảng đổi identity mỗi lần sync, kể cả read receipt, sẽ giật viewport người đang đọc ngược lịch sử); cột avatar luôn giữ chỗ để run tin nhắn không nhảy ngang; lỗi "users don't exist" **dịch** thành `chat.peerNotReady` chứ không phơi chuỗi SDK, và **giữ vĩnh viễn** vì vẫn xảy ra với người không phải bạn bè. Sửa luôn dòng ledger của `chat` bị lệch một cột từ P2.7a (thêm ô boundary note mà không lấp ô rỗng sẵn có) — nay 11 ô khớp mọi dòng khác.|

| 2026-07-28 | P2.7c-2 | **Cửa sổ chat nổi của app shell (tách 3/4).** 2 component mới, dưới trần: `ChatDock` (nút mở + panel danh sách + hàng cửa sổ) + `FloatingChatWindow`. **Không chép lại gì**: cửa sổ nổi dùng thẳng `ConversationView`/`ConversationSidebar`/`MessageBubble`/`MessageComposer` của c-1 — đúng lý do c-1 cho chúng nhận props thay vì tự gọi hook. Thứ duy nhất thêm vào component cũ là **một slot** `actions` trên header `ConversationView`, chọn slot chứ không phải hai prop `onMinimize`/`onClose` vì nút nào có mặt là việc của khung đang bọc (`/chats` không cần nút nào), và bộ prop kiểu đó sẽ đẻ prop thứ ba vào ngày có khung thứ ba. **CÔNG BỐ ở đầu bước**: `ChatClientProvider` mount **một lần** ở `app/(main)/layout.tsx` — nghĩa là mọi trang trong `(main)` mở websocket + gọi `/chat/token`; vẫn chọn toàn cục vì badge tổng tin chưa đọc của dock Twilio hiện **sống trên mọi trang**, mount lười sẽ giết badge = hồi quy (Guardrail C). **Việc mount để ở d chứ không làm ở đây**: layout còn phải giữ `CommunicationProvider` tới khi `/chats` được rewire (trang đó gọi `useCommunication()`), nên gỡ Twilio + gắn Stream vào shell là **một** thao tác thuộc **một** commit; tách đôi sẽ để lại một checkpoint mà cả hai nhà cung cấp cùng connect trên mọi trang. **Đối chiếu Guardrail C từng hành vi của `chat-box.tsx` + `communication-provider.tsx`**: giữ tối đa 3 cửa sổ (`.slice(-3)`), dedupe mở-lại-thì-khôi-phục, thu nhỏ thành pill avatar, đóng, ẩn toàn bộ dock khi ở `/chats`, badge tổng chưa đọc. **Bỏ có chủ đích 2 thứ**: (1) chấm xanh + chữ "Đang hoạt động" — legacy vẽ **cứng trong markup**, không đọc từ đâu, người offline hai tuần vẫn hiện đang hoạt động; Stream có presence thật nhưng bật là thêm một mặt dữ liệu vào checkpoint đã đủ việc, ghi nợ thay vì bê nguyên lời nói dối sang bản mới; (2) icon Messenger của Facebook + gradient `#0099ff→#a033ff` — mượn thương hiệu người khác và là màu cứng ngoài mọi token nên dark mode không đổi theo (**DS deviation #22**, cùng khoảng trống "DS không có specimen chat" đã ghi ở #21). Kiểm `/chats` đặt **trong** `ChatDock` chứ không ở chỗ mount (là sự thật về component, không phải về layout) và **sau** mọi hook để không đổi thứ tự hook giữa route. Mỗi cửa sổ tự gọi `useConversation` vì dock giữ mảng cửa sổ **dài thay đổi được** — gọi hook cho từng phần tử mảng đó là thứ React cấm. **Verify trên BE + Stream thật** (route preview tạm, đã xoá): panel 2 hội thoại thật · cửa sổ 9 tin lịch sử tự cuộn đáy · gửi tiếng Việt có dấu → **một** bong bóng phải, ô nhập tự xoá · 2 cửa sổ cạnh nhau · mở lại hội thoại đang mở → vẫn **2** (dedupe đúng) · thu nhỏ ↔ khôi phục · **bơm tin đến bằng user token của 9004** (công thức c-1): tin tới khi cửa sổ **ĐANG MỞ** → hiện realtime, badge **không** lên; tin tới khi **ĐÃ ĐÓNG** → badge `1`, mở ra → về rỗng · light + dark đúng token · console sạch, lỗi duy nhất là `[Twilio] Initialization failed` có sẵn. **Chưa kiểm được trần 3 cửa sổ ở biên** — chỉ có 2 hội thoại vì Neo4j còn đúng 1 tình bạn sau lần reset DB; `.slice(-MAX_WINDOWS)` là bản sao đúng của legacy và dedupe đã đo, nhưng cảnh "mở cái thứ 4 đẩy cái cũ nhất" chưa chạy qua. **Bẫy môi trường khi đo**: dock Twilio còn mount nên hai nút tròn **chồng đúng lên nhau** ở góc phải dưới, legacy `z-50` > bản mới `z-40` → click theo toạ độ luôn trúng nút Twilio; ẩn tạm nút legacy bằng JS rồi click theo `aria-label`. Dev DB trả nguyên trạng (`email_verified` của 9004 về `false`).|

| 2026-07-28 | P2.7d | **Wiring + xoá legacy — chat ĐÓNG DOMAIN (1/1, cả 4 cột).** 1 màn (`/chats`) + 1 component mới (`ChatMessenger`), dưới trần. **Twilio chết hẳn**: `components/chat/` 6 file + `lib/twilio/` 4 file + `app/api/twilio/token` + 2 dependency (`twilio`, `@twilio/conversations`) + 2 key i18n (`chat.activeNow` là presence bịa, `chat.you` hết caller) — ~1830 dòng, lần xoá lớn nhất của cuộc di trú. **`src/app/api/` không còn tồn tại**: đây là lần duy nhất xoá **một backend nằm trong FE** — route handler đó tự ký JWT bằng `TWILIO_API_KEY_SECRET`, tức FE vừa giữ secret vừa cấp quyền, đúng thứ §4 cấm (ghi từ R7, nay đóng). `ChatClientProvider` mount **một lần** ở `app/(main)/layout.tsx` với `enabled={Boolean(profile) && profile?.role !== 'ADMIN'}` — chờ hồ sơ để không đua với việc dựng phiên, và admin (bị effect ngay trên đó đá sang `/admin/moderation`) không mở socket nào. Đo được: **1 websocket + 1 request cấp token mỗi lần tải trang**, **0** thêm khi điều hướng client-side. **Lỗi thật bắt được NHỜ mount toàn cục**: trước khi sửa, mỗi lần tải trang xin token **2 lần** cho **một** connection — SDK gọi hàm cấp token **ngay trong** `connectUser`, nên request đầu chỉ để đọc `apiKey`/`userId`. Ở route preview thì đó là chuyện một trang; vào shell thì thành mỗi trang một lần thừa. Sửa: lần gọi đầu trả lại chính credential vừa lấy (`credentialSpent`), mọi lần sau vẫn đi mạng nên vẫn giữ nguyên lý do phải truyền hàm thay vì chuỗi (hết 24h SDK tự xin lại). Đo lại **2 → 1**. `TokenProvider` bắt buộc `Promise<string>` chứ không nhận union → hàm phải `async`; **`yarn build` bắt được, `tsc` không** (build chạy type-check của Next trên cả `.next/types`). **Bỏ hai con số phỏng đoán về chiều cao**: bản Twilio tự tính `h-[calc(100vh-48px)]` mobile / `h-screen` desktop, **cả hai đều sai** (thanh tìm kiếm **65px**, header mobile **57px**) nên trang luôn có thanh cuộn thừa — đo ra `scrollHeight 790 > innerHeight 782`. Sửa bằng cách **chia không gian thay vì đo nó**: route full-bleed thì shell là flex column `h-[100dvh]`, chrome `shrink-0`, nội dung `flex-1 min-h-0`, `ChatMessenger` chỉ cần `h-full` — không còn hằng số nào để sai, và các route khác giữ `min-h-screen` để bảng tin vẫn cuộn. Không phải dựng lại shell (P3.4 vẫn làm): 4 class gắn với đúng một route, và nó **xoá** hằng số chứ không thêm. **`?sid=` → `?c=`**: id vẫn nằm trên URL (deep-link + back của trình duyệt là hành vi legacy phải giữ) nhưng đổi tên vì id Stream không phải SID Twilio — link cũ chết sạch dù có giữ tên hay không, giữ tên chỉ để thanh địa chỉ nói tên một nhà cung cấp app không còn gọi; nút back **xoá tham số** chứ không `router.back()` vì mở thẳng link hội thoại thì phía sau không có `/chats` nào. **Một-pane mobile suy từ URL**: màn Twilio giữ `mobileView` **song song** với URL nên hai nguồn lệch được (mở link hội thoại trên điện thoại vẫn hiện danh sách); bản mới chỉ hỏi "URL có hội thoại không". Nút back ẩn ở desktop qua `data-slot="back"` + `md:[&_[data-slot=back]]:hidden` — "ở breakpoint nào" là câu hỏi của CSS, JS không trả lời được lúc render, nên không thêm prop cho nó. **Verify trên chính `/chats` và shell thật, không route tạm**: dock trong shell có đúng **một** nút mở (nút Twilio biến mất) · **console sạch mọi trang** — `[Twilio] Initialization failed` đã hết · chọn hội thoại → URL `?c=!members-…` · **reload thẳng link** → mở đúng hội thoại, hàng active sáng · gửi tiếng Việt có dấu → sidebar đổi preview sang "Vừa xong" · dock **không render** trên `/chats` · `scrollHeight === innerHeight` · **417px**: mở hội thoại → danh sách `display:none`, hội thoại rộng 417, mũi tên back hiện; bấm back → URL sạch tham số, danh sách trở lại full width · light + dark. **Cách đo responsive khi không resize được cửa sổ**: `resize_window` báo thành công nhưng `innerWidth` vẫn 1536 (cửa sổ maximize); đường vòng đo được thật là **nhét một `<iframe>` cùng origin rộng 420px trỏ vào chính `/chats`** rồi đọc `getComputedStyle` bên trong — media query trong iframe theo viewport của iframe, rẻ và chắc hơn đọc class bằng mắt. **Nợ ghi lại**: presence thật chưa bật (bản cũ vẽ chấm xanh cứng, bản mới không vẽ gì — Stream có `user.presence_changed`, muốn thì là checkpoint riêng) · trần 3 cửa sổ nổi vẫn chưa chạy qua ở biên (chỉ 2 hội thoại vì Neo4j còn 1 tình bạn) · `queryChannels` giới hạn 30, chưa có UI nạp thêm · shell vẫn legacy shadcn cho mọi thứ ngoài chat → P3.4.|

| 2026-07-28 | P2.8 | **Domain `search` (tier C, 1 checkpoint cả 4 lớp) — ĐÓNG (1/1, cả 4 cột).** 1 màn (`/search`) + ô tìm kiếm của shell, **4 component mới** (`SearchBar`, `SearchResults`, `UserResultCard`, `PostResultCard`; `BookLine` để cục bộ không export vì mới có một caller) — dưới trần 2 màn / 5 component, không phải tách. Dựng `features/search/`: `types` derive từ `schema.gen.ts` + `api` (1/1) + `hooks` (`searchKeys`, `useSearch`, `MIN_QUERY_LENGTH`) + `components` + barrel. **Đo endpoint trước khi thiết kế, không suy từ tên**: `?q=` rỗng → **422** (không phải 400), thiếu `q` → 400, `size=0/-1` → **422**, không truyền `size` → mặc định **10**, `q=%` → **0 kết quả** (sanitizer escape wildcard LIKE), và `q=tran` khớp `Trần` — `unaccent` hai phía nên **không dấu vẫn tìm được**. `size` là trần của **từng** danh sách nên `size=20` có thể trả 20 người **và** 20 bài. **Không cursor, không xếp hạng**: substring LIKE trên 3 bảng Postgres, không index tìm kiếm — nên UI cố ý không ngụ ý thứ hạng (không "kết quả hàng đầu", không điều khiển trang), thứ tự section cố định người→bài là cách trình bày trung thực. Sách **không** là danh sách thứ ba: sách khớp nổi lên dưới dạng bài gắn nó, và sách không gắn post thì **không bao giờ xuất hiện**. **Lỗi BE thứ nhất bắt được khi nhìn màn hình — S1**: bài vừa insert xong hiện **"7 giờ trước"**. Nguyên nhân: `search/dto/PostDto.createdAt` là **`LocalDateTime` — DTO `createdAt` DUY NHẤT trong toàn bộ backend** khai như vậy (grep cả `src/main/java`: mọi DTO khác là `OffsetDateTime` và gửi kèm `Z`, ví dụ `/notifications` trả `…362961Z`), nên Jackson gửi chuỗi **không zone** và `new Date(...)` đọc nó là giờ **địa phương** → lệch đúng bằng offset máy (UTC+7). Sửa đúng là một từ ở BE; FE tạm né bằng `withAssumedUtc` (gắn `Z` khi chuỗi không có zone) — **workaround có giả định** (DB lưu UTC, đã kiểm `timestamptz` mọi hàng `+00`), ghi rõ là BE sửa xong thì hàm thành no-op và **phải xoá**. Đo lại sau khi né: "7 giờ trước" → **"2 phút trước"**. **Lỗi BE thứ hai — S2**: `PostDto` khai 6 khối details (`quizDetails`/`codeSnippetDetails`/`articleDetails`/`qnaDetails`/`pollDetails`/`linkDetails`) nhưng `SearchService.toPostDtos` **không set cái nào** — mọi post trả về đều null cả 6. **Đúng lỗi `fanOutPost` của `findings/posts.md`, ở service thứ hai.** FE **loại hẳn 6 key khỏi `SearchPost`** thay vì khai always-null: một key không bao giờ mang giá trị không phải dữ liệu để component đi rẽ nhánh. **Xoá legacy**: `components/search/` (cả thư mục), `lib/api/search.ts`, `lib/hooks/use-search.ts`, 4 type trong `lib/types` (thay bằng bia mộ tại chỗ), 2 dòng barrel → **`src/lib/api/` và `src/lib/hooks/` còn 10 file mỗi thư mục**. **`useDebouncedValue` + `useDebouncedSearch` xoá HẲN chứ không migrate** — grep ra **0 caller**; code chết thì xoá outright (Constraint #2), ngày có search-as-you-type thì viết debounce theo caller thật. Type cũ sai **3 chỗ**, ghi trong bia mộ: thiếu hẳn `eliteScore`, khai mọi field non-nullable trong khi `avgRating`/`price` là null với sách chưa đánh giá (**trang legacy gọi `.toFixed(1)` không guard → vỡ cả danh sách kết quả**), và bỏ 6 khối details. **Quyết định**: ô tìm kiếm thuộc `features/search` **chứ không phải `shared/`** (tìm kiếm là khái niệm domain; để vào `shared/` là mở màn cho bucket toàn cục kế tiếp, §4); **submit bằng Enter, không search-as-you-type** (mỗi phím là một truy vấn LIKE không index, và dropdown sống chính là command palette của P3.4) — giữ đúng hành vi legacy, Guardrail C; **kết quả là ngõ cụt có chủ đích** vì BE không có endpoint profile công khai và app không có route chi tiết bài, ghi ở cả hai card để người sau không "sửa" bằng cách ship link 404. **Nút xoá KHÔNG đi qua prop `suffix` của `Input`**: `Input` bọc adornment trong span `aria-hidden` — đúng cho icon trang trí, **sai cho một control** (vô hình với screen reader mà vẫn ăn tab stop) — nên đặt tuyệt đối đè lên field, `pr-9` chừa chỗ. **`Input` của `shared/` nay nhận `ref`** (prop thường, React 19 bỏ `forwardRef`), thêm vì có caller thật: Escape thì blur, xoá xong thì trả focus. **Verify trên BE thật** với fixture insert thẳng (`t_posts` + `t_books`, không chạm Gemini): 4 loại card · sách hiện inline với `4.5 · 150.000 ₫` · **sách miễn phí `avg_rating = null` hiện "Miễn phí", không sao, không vỡ** (đúng ca legacy crash) · `q=kien truc` khớp "kiến trúc" · `q=tran` khớp "Trần Khôi" · người hiện handle + `RepScore` · empty state `>_` · **nhánh lỗi hiện "Tìm kiếm thất bại" chứ không phải "không có kết quả"** (bài học P2.6cd: skeleton khi `pending && fetching`, lỗi khi `status !== success`, empty **chỉ khi** success) · light + dark · console sạch. **Mẹo đo nhánh lỗi**: patch `XMLHttpRequest.prototype.open` đổi URL sang 404 rồi điều hướng **client-side** qua chính ô tìm kiếm — `navigate` sẽ reload và xoá mất patch, và axios dùng **XHR chứ không dùng `fetch`** nên patch `window.fetch` không ăn. Dev DB trả nguyên trạng (xoá hết fixture, về đúng 2 post / 0 sách). **Quan sát ghi nợ, không sửa ở đây**: `features/newsfeed/types/feed.ts` dùng `Schemas[...][K] | null` nên value type còn lọt `| undefined` — chính chỗ P2.8 phải đổi thành `Required<Schemas[...]>[K] | null` mới compile được; feed chưa vấp vì chưa gọi method trên field nullable, rà lại ở P3.1.|

| 2026-07-28 | P2.9 | **Domain `trending` (tier C, 1 checkpoint cả 4 lớp) — ĐÓNG (1/1, cả 4 cột).** 1 màn + **3 component mới** (`TrendingCard`, `TrendingFilters`, `TrendingList`), dưới trần. Dựng `features/trending/`: `types` derive từ `schema.gen.ts` + `api` (1/1) + `hooks` (`trendingKeys`, `useTrending` infinite) + `components` + barrel. **Domain duy nhất mà nội dung không do người dùng app viết**: crawler kéo từ HN/dev.to/GitHub/Reddit/Medium/HBR theo lịch, Gemini phân loại, app không tạo/sửa/react — mỗi card là link **rời khỏi app**, đó là toàn bộ mô hình tương tác. Cũng là **domain duy nhất có sẵn dữ liệu thật** (110 hàng) nên không phải dựng fixture. **Đo endpoint trước khi thiết kế**: phân trang **thật** (`page/size/totalElements/totalPages/hasNext`) khác hẳn `search` · `category=NOPE` → **400** (bind vào enum Java) nhưng **`timeRange=decade` → 200 và im lặng trả về "week"** (là `String` thô đưa vào `switch` có nhánh `default`) → vì vậy `TrendingTimeRange` là **union đóng ở FE**, đây là chỗ duy nhất bắt được lỗi đó · `page=0`/`size=0` → **400**, trong khi **cùng loại vi phạm ở `search` là 422** — hai controller đi hai đường validation, code xử lý lỗi không được giả định một mã. **Lỗi BE bắt được — T1, và là lỗi tốn tiền**: `TrendingItemDto` khai `tags`, `TrendingClassificationService` **thật sự hỏi Gemini** đúng cấu trúc `{"category":…,"tags":[…]}`, nhưng `TrendingCrawlScheduler` chỉ lấy lại category (`categories.get(i)` → `saveItem(crawled, category)`) và **không bao giờ `setTags`** — tức quota Gemini đã bị tiêu để sinh tags rồi ném đi. Đo **toàn bảng, không lấy mẫu**: `110/110` hàng tags rỗng. FE **loại hẳn `tags` khỏi type**, đúng cách đã làm với 6 khối details của `search`; card legacy có hàng tags điều kiện **không thể hiện ra trên bất kỳ hàng nào trong 110**. **T2 (quan sát, chưa kết luận)**: 3/6 crawler chưa từng sinh dữ liệu (`REDDIT`/`MEDIUM`/`HBR` = 0 hàng; chỉ HN 43 · GITHUB 34 · DEV_TO 33) và chỉ 6/8 category xuất hiện — **không cắt filter** vì crawl ngày mai có thể lấp vào. **Quyết định**: bộ lọc là **state của component chứ không phải URL** (khác `/search`) — từ khoá là thứ người ta chia sẻ và quay lại, còn "mã nguồn mở, tuần này" là cách lướt không ai gửi link; đưa lên URL là thêm mối lo routing để đổi lấy nút share không ai xin · category là **chip chứ không phải `Tabs` thứ hai** (9 lựa chọn thì tab phải cuộn ngang hoặc bóp nát nhãn) · **cả card là link ra ngoài** `rel="noopener noreferrer"` + mũi tên chéo, nói thẳng thay vì làm card trông bấm được trong app · **`hasNext` của payload quyết định dừng** chứ không dùng mẹo "trang cuối ngắn" (BE tính từ total thật nên đúng ở đúng biên mà mẹo kia sai) · `staleTime` 5 phút vì dữ liệu không thể đổi nhanh hơn lịch crawl · **nút "Thử lại" giữ kèm cảnh báo** về lỗi chưa giải thích được ở `findings/notifications.md` §14. **`publishedAt` ở đây là `OffsetDateTime` có `Z`** nên `useRelativeTime` đọc đúng — **không cần** workaround S1 của `search`, đã ghi ngay trong `TrendingCard` để người sau không copy nhầm sang. **Xoá legacy**: `components/trending/` (cả thư mục), `lib/api/trending.ts`, `lib/hooks/use-trending.ts`, 5 type trong `lib/types` (thay bằng bia mộ), 2 dòng barrel → **`lib/api/` và `lib/hooks/` còn 9 file mỗi thư mục**; `src/components/` nay chỉ còn `moderation/`, `posts/` (cầu tạm bookstore), `ui/` (shadcn) và `service-worker-register.tsx`. Đóng nốt **dòng nợ P2.6cd**: `trending-card.tsx` là file legacy thứ hai phải sửa theo khi i18n dời sang `time.*` — nó chết ở đây, và bản mới dùng thẳng `useRelativeTime` thay cho bản sao 18 dòng tự viết. Bản cũ **chép tay cả hai enum** (14 giá trị phải nhớ đồng bộ với Java bằng tay). **Verify trên dữ liệu thật**: lọc category ra **đúng dây** — "Tất cả" gửi **không có** tham số `category`, chọn chip gửi `category=OPENSOURCE` (đọc từ `performance.getEntriesByType`) · lọc `timeRange=today` · **infinite scroll 20 → 34 card đúng bằng `totalElements` của OPENSOURCE rồi DỪNG HẲN** (không có request thứ 4) + hiện "Bạn đã xem hết" · `rel` đúng · **empty state thật** bằng category `EVENT` (0 hàng) · **nhánh lỗi hiện "Không tải được xu hướng" chứ không phải "chưa có nội dung"** · light + dark · console sạch. **Extraction test sạch nhất dự án**: `features/trending` **không import feature nào khác** — item crawl không có tác giả trong hệ thống nên không identity row, không Elite Score, không đồ thị bạn bè; chỉ `core/`, `shared/`, `@/lib/i18n`. **Đính chính ghi chú P2.5**: `findings/newsfeed.md` ghi "tab ẩn thì IntersectionObserver KHÔNG chạy" — lần này IO **có chạy** dù `visibilityState === 'hidden'` (cuộn bằng thao tác cuộn thật của extension), nên triệu chứng cũ nhiều khả năng do nguyên nhân khác; đừng dùng "tab đang ẩn" làm lời giải thích mặc định. Dev DB **không bị đụng vào**.|
| 2026-07-29 | P2.12 | **Đồng bộ lại hợp đồng sau đợt bugfix BE 28–29/07 (22 commit, B1–B18 xong 17/18).** Sinh lại `schema.gen.ts` từ BE `18efb6c`: **+250/−171**, 90→**91 path**, 101→**102 operation**. `tsc` gãy **9 lỗi / 4 file** — đúng thứ cơ chế này dựng ra để bắt, không phải 404 lúc chạy. Sửa: `ProfessionalProfile` và `NotificationPreference` derive sang DTO mới (entity bị gỡ khỏi spec theo QĐ-0002), `EventRsvp`→**`EventAttendee`** (shape đổi thật: mất `id`/`postId`, `createdAt`→`respondedAt`, **thêm `fullName`+`profilePictureUrl`** nên danh sách người tham gia hiện hiển thị được), `getAttendees` nhận thêm `status` lọc server-side, gỡ `POST_SHARED`/`SYSTEM` (BE xoá khỏi enum), **thêm `EVENT_RSVP`+`EVENT_REMINDER` vào `MUTABLE_TYPES`** vì giờ đã có nơi phát. **Sửa lỗi đang gãy thật trên `/newsfeed`**: `acceptAnswer` PATCH→POST (BE trả 405 kèm `Allow: POST`), thêm `unacceptAnswer` (`DELETE`) → posts **22→23 endpoint**, UI gỡ chọn còn nợ. **Verify trên BE thật**: POST accept → `{isResolved:true, acceptedAnswerId:21}` → DELETE → về `null` (chứng minh cả B10 lẫn cache Redis được cập nhật); `PUT emailFrequency=WEEKLY_DIGEST` → 400; `V45` ok=true và CHECK constraint chặn thật (thử ghi rồi rollback); feed thật không còn `shareCount`, có `likeCount`/`commentCount`/`authorLevelName`. Dev DB đã trả về trạng thái ban đầu. **Bắt được 2 chỗ `fe-debt.md` của BE ghi sai**: sót `EventRsvpEntity` (7 entity bị gỡ, không phải 6) và `POST_SHARED` **có** vỡ compile. **Lỗ quy trình**: đếm operation không thấy 5 endpoint đổi method/path — phải so từng cặp `(path, method)`, xem `schema-drift.md`. |

---

| 2026-07-30 | F-A | Gỡ 3 workaround giấu dữ liệu BE đã trả: `KINDS_WITH_UNECHOED_DETAILS`+`canEdit` (`feed-post.tsx`), `acceptedInSession` (`comment-thread.tsx`), union `NeverPopulated` 6 khối (`search/types`). Truyền lại `commentCount`/`likeCount`. Dựng `useUnacceptAnswer` + prop `onUnacceptAnswer` (N5). Xoá literal chết `shareCount` (N4). **Đo trước khi gỡ**: feed trả codeSnippet 2/2, qna 1/1, book 1/1 non-null; `commentCount`=1 thật; search trả `qnaDetails` non-null. Verify preview: chọn→"Bỏ chọn đáp án"→gỡ, POST+DELETE đều 200. Giữ prop `canEdit` (hợp lệ cho lý do khác), chỉ bỏ lý do đang truyền. |
| 2026-07-30 | F-B | `tags` trending trở lại type + hàng tag có điều kiện (census **97/310** dòng có tag; trang 1 rỗng 20/20 nên mẫu đầu đánh lừa — item đầu có tag ở vị trí 28). Bỏ cảnh báo `linksNotSaved` **và** gửi kèm `externalLinks` trong payload lưu (DTO đã có field) — bỏ cảnh báo mà không gửi thì biến hạn chế công khai thành hạn chế thầm lặng. Phát hiện thêm: `QuizTaker` gõ theo DTO **tác giả** (`QuizDetails`) trong khi payload đọc là `PublicQuizDetailsDto` → thêm alias `PublicQuizDetails`, và `explanation` đọc từ `result.explanations[i]` (nhánh cũ không bao giờ chạy được). |
| 2026-07-30 | F-C | Xoá `withAssumedUtc` (DTO đã `OffsetDateTime`). `resolved` ở `qna-body` rút về `isResolved` đơn thuần (đo round-trip post 91: accept→`isResolved:true`, undo→`false`). Dựng `EventAttendeeList` (component MỚI) + lọc `?status=` phía server; thêm `postKeys.attendeesAll` vì `status` vào key làm invalidate cũ hụt mọi tab đã lọc. Thanh Elite Score đổi sang `currentLevelMin`→`nextLevelMin`. Bỏ 3 ghi chú "ảnh bìa chết sau 24h" (BE presign lúc đọc — `X-Amz-Date` là ngày hiện tại trên sách đăng 2 ngày trước). Render 5 khối details trong kết quả tìm kiếm (gộp từ F-A vì cùng file). |
| 2026-07-30 | F-D | Xoá 3 khoá i18n nguồn trending chết (`REDDIT`/`MEDIUM`/`HBR` — enum BE còn 3) + sửa 3 comment nói "sáu nguồn". Bỏ nghi thức dựng graph Neo4j tay khỏi `session-constants` + `findings/friendships` (BE seed mỗi `compose up`; đo: 9001 có 12 bạn, suggestions trả **10** thay vì `[]`) — giữ nguyên văn công thức cũ làm lịch sử/chẩn đoán kèm cảnh báo đừng chạy lại. Ghi QĐ-0001 vào `findings/notifications` (poll 30s là thiết kế cuối, không phải giải pháp tạm). Ghi B19 vào `be-requests`. |
| 2026-07-30 | P2.13a (roadmap 2a) | Data layer domain `roadmap` (8/8 endpoint, 2 controller, 1 module). Type là **mapped type trên `schema.gen.ts`**, chỉ siết nullability theo Java. **PHÁT HIỆN LỚN — B20**: cả 5 endpoint `@PreAuthorize` đều **không có hiệu lực** (không có `@EnableMethodSecurity`; `SecurityConfig` chỉ gác `/v1/api/admin/**`) — seed user thường `POST /roadmaps` **200**, JWT không có claim role. **B21**: không có endpoint đọc tiến độ của chính mình (`findByUserId` có sẵn, không controller nào gọi) + `verify` trả `void` → 200 không nghĩa là được duyệt, `AUTO_CERTIFIED` trượt cũng 200. Đo thêm: nodes **không `OrderBy`** (sort là việc consumer); `POST .../nodes` **echo request DTO** nên `orderIndex` null trong khi GET trả 0 → tách type `CreatedRoadmapNode`; `{id}/nodes` id sai → **200 `[]`** không phải 404; `name` rỗng → **422** kèm `details`; bảng roadmap **rỗng, không có seed**. Dữ liệu thử đã xoá bằng SQL (không có endpoint DELETE). tsc + lint + prettier sạch. |
| 2026-07-30 | P2.13b (roadmap 2b) | State layer: `roadmapKeys` (2 nhánh độc lập — catalogue vs hàng chờ duyệt, không invalidate lẫn nhau) + 9 hook. **Sort nằm ở `useRoadmapNodes` qua `select`** vì BE không `OrderBy`; tiebreak theo `id` vì `orderIndex` mặc định 0 cho cả roadmap nên thiếu tiebreak là quay lại đúng thứ tự bất định cần tránh. `useSubmitVerification` **cố ý không invalidate gì**: không có endpoint đọc tiến độ (B21), còn hàng chờ thì user thường không được đọc. Reputation không invalidate — cache domain khác (§4). **Phát hiện mới**: `UserRole` BE là `{USER, ADMIN}`, **không có `MODERATOR`** → `hasAnyRole('ADMIN','MODERATOR')` gọi tên role không ai mang được; đã bổ sung vào B20, và `useIsRoadmapAdmin` gác bằng ADMIN. Cổng đọc `role` qua barrel `features/security`, **không** dùng `lib/hooks/use-admin-role.ts` legacy (findings §1 bản 2a ghi sai, đã sửa). Đo: 9001 là `USER`; DB có 2 ADMIN (id 6,7) nhưng **không biết mật khẩu** → công thức nâng/hạ quyền bằng SQL cho 2c/2d đã thử và trả lại nguyên trạng. tsc + lint + prettier sạch. |
