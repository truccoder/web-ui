# FE Migration Ledger — trạng thái

Sổ cái theo dõi việc di trú FE sang kiến trúc microservices-ready
(`src/features/<domain>/` mirror 1:1 package BE). Luật ở `CLAUDE.md`, thứ tự task ở
`docs/prompts/wbs-theo-giai-doan.md`.

**Cập nhật sổ này ở bước `d` / `cd` của mỗi domain.** Một domain chỉ "xong" khi mọi
endpoint BE có hàm API tương ứng **và** ít nhất một surface UI tiêu thụ nó.

Trạng thái: `not started` · `in progress` · `done` · `n/a`

## File nào đọc lúc nào

File này là **thứ duy nhất đọc ở đầu mọi phiên**. Phần còn lại đọc theo nhu cầu — sổ cái được
tách ra chính vì đọc trọn gói tốn context mà phần lớn không dùng tới.

| file                                                           | đọc khi nào                                                            |
| -------------------------------------------------------------- | ---------------------------------------------------------------------- |
| **file này** (bảng tiến độ + bản đồ route)                     | **luôn luôn**, đầu phiên                                               |
| [`ledger/findings/<domain>.md`](ledger/findings/)              | **trước khi làm domain đó** — nợ kỹ thuật, cạm bẫy, quyết định đã chốt |
| [`ledger/be-requests.md`](ledger/be-requests.md)               | khi gửi/theo dõi yêu cầu sửa cho BE — gom B1–B17 từ mọi findings       |
| [`ledger/ds-deviations.md`](ledger/ds-deviations.md)           | khi dựng UI từ Design System                                           |
| [`ledger/legacy-inventory.md`](ledger/legacy-inventory.md)     | ở bước `d` (xoá legacy)                                                |
| [`ledger/checkpoint-log.md`](ledger/checkpoint-log.md)         | **hiếm khi** — append-only, chỉ để truy lại lịch sử                    |
| [`ledger/schema-drift.md`](ledger/schema-drift.md)             | chỉ khi drift check ra diff khác rỗng                                  |
| [`prompts/session-constants.md`](prompts/session-constants.md) | đầu phiên, thay cho việc dán lại vào prompt                            |

Domain đã có file findings: `posts` · `newsfeed` · `security` · `friendships` · `reputation` · `notifications` · `chat` · `search` · `trending` · `bookstore` · `knowledge` · `roadmap` · `moderation` · `github` · `matchmaking` · `shared`
(hạ tầng, không thuộc domain nào). Domain mới thì tạo file mới khi có phát hiện đầu tiên.

---

## 1. Ledger — tiến độ di trú

Các cột theo dõi việc chuyển vào `src/features/`. Câu dẫn cũ ("chưa có gì trong `features/`
nên tất cả đang là `not started`") viết từ P0 và đã hết hiệu lực — 13/15 domain đã đóng.
**Khối B đã chạy hết cả 4 domain.** `roadmap` đóng ở P2.13d; `moderation` ở P2.15cd; `github` ở P2.14cd **ở mức mặt đọc** (4/5 endpoint có UI, `linkAccount` hoãn — B23a); `matchmaking` ở P2.16ab **chỉ tới data/state** (UI hoãn — B24 khiến domain không dựng được màn nào). Hai khoản hoãn đều có lý do và theo dõi ở P4.7.

| domain        | BE package                    | #ep | boundary note                                                                                                                                                                                                                                                                                                                                                                                                                      | types            | api                            | store/hooks       | UI                                                                                                                                         | wired                                       | legacy removed                                                                                                                                               | verified           |
| ------------- | ----------------------------- | --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ------------------------------ | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------ |
| posts         | `com.socialapp.posts`         | 23  | `EventController` (`/events`) nằm trong package `posts` — mirror theo package, không tách `features/events/`. `acceptAnswer` nằm ở `PostController` chứ không phải `CommentController` → thuộc chu kỳ 1. **22 → 23 (2026-07-29)**: BE thêm `DELETE /qna/accept-answer` (`c272cd3`) và đổi `acceptAnswer` PATCH → POST (`39b5666`); hàm API đã có, **UI gỡ chọn đáp án còn thiếu**                                                  | **done (23/23)** | **done (23/23)**               | **done (23/23)**  | **done (23/23)** (composer 8/8 loại + toàn bộ mặt đọc + RSVP/lịch + gỡ chọn đáp án + danh sách người tham gia có lọc — khối A, 2026-07-30) | **done** (`/newsfeed`)                      | **done** (bookstore giữ cầu tạm → P2.10)                                                                                                                     | **done**           |
| security      | `com.socialapp.security`      | 17  | `/profile/professional` (GET+PUT) **không** thuộc security — `ProfessionalProfileController` nằm trong package `knowledge`. Lọc path `/v1/api/profile*` sẽ ra 19, đúng là 17.                                                                                                                                                                                                                                                      | done (17/17)     | done (17/17)                   | done (17/17)      | done (all surfaces)                                                                                                                        | done                                        | 1+2+3 (shell còn dùng useProfile+role → P3.4)                                                                                                                | done               |
| bookstore     | `com.socialapp.bookstore`     | 11  | `PaymentController` (`/payments`) nằm trong `bookstore`; route `/payment/success` đứng riêng. `POST /posts/books` (tạo sách) **không** thuộc đây — ở `PostController`, đã tính trong 22 của `posts`. `POST /payments/momo/webhook` là MoMo gọi server-to-server → **cố ý không có hàm FE** (10 hàm / 11 endpoint)                                                                                                                  | **done (11/11)** | **done (10/11 + webhook n/a)** | **done (10/10)**  | **done** (đọc/mua · đánh giá · kết quả thanh toán)                                                                                         | **done** (`/newsfeed` + `/payment/success`) | **done** (2 file cầu tạm, `lib/api/books.ts`, `lib/api/payments.ts`, `lib/hooks/use-books.ts`, `use-payments.ts`, 9 type — `src/components/posts/` biến mất) | **done**           |
| knowledge     | `com.socialapp.knowledge`     | 10  | `PersonalAccessTokenController` (`/tokens`) và `ProfessionalProfileController` (`/profile/professional`) nằm trong `knowledge` dù path trông như security. **`/knowledge/sync/pull` + `push` xác thực bằng Personal Access Token, KHÔNG bằng JWT phiên** (đo: JWT → 404 "Invalid token") → dành cho client Obsidian bên ngoài, **cố ý không có hàm FE**                                                                            | **done (10/10)** | **done (8/10 + 2 n/a)**        | **done (9/9)**    | **done** (hồ sơ · token · giải thích + thư viện)                                                                                           | **done** (`/knowledge`)                     | **n/a** (chưa từng có code legacy)                                                                                                                           | **done**           |
| friendships   | `com.socialapp.friendships`   | 8   |                                                                                                                                                                                                                                                                                                                                                                                                                                    | done (8/8)       | done (8/8)                     | done (8/8)        | done (3 màn)                                                                                                                               | done                                        | done (phần của 4 route; `/chats` đóng ở P2.7d — sidebar chat gọi `useFriends` qua barrel; dashboard/shell → P3.3/P3.4)                                       | done               |
| roadmap       | `com.socialapp.roadmap`       | 8   | **Hai controller, một module**: `RoadmapController` (`/roadmaps`, 4) + `SkillVerificationController` (`/skills`, 4) — cùng package BE, và một skill verification là claim về **một roadmap node**, nên `/skills` không tách thành feature riêng. **5/8 endpoint gác quyền bằng `@PreAuthorize` KHÔNG có hiệu lực** (B20) và **không có endpoint đọc tiến độ của chính mình** (B21) — xem `findings/roadmap.md`                     | **done (8/8)**   | **done (8/8)**                 | **done (9 hook)** | **done (5 component)** — duyệt: list/tree/form · admin: hàng chờ + soạn thảo                                                               | **done** (`/roadmap` + `/admin/roadmap`)    | **n/a** (chưa từng có code legacy)                                                                                                                           | **done**           |
| notifications | `com.socialapp.notifications` | 6   | BE **không có realtime nào** (không WebSocket, không SSE) → badge chưa đọc chỉ poll 30s được. Xem `findings/notifications.md`                                                                                                                                                                                                                                                                                                      | done (6/6)       | done (6/6)                     | done (6/6)        | done (list + panel tuỳ chọn)                                                                                                               | done (`/notifications`)                     | done                                                                                                                                                         | done               |
| github        | `com.socialapp.github`        | 5   | **Luồng LIÊN KẾT bị chặn cứng (B23)**: `/github/oauth/url` và `/auth/github/url` trả chuỗi **giống hệt nhau** (chung `GithubApiClient` + một khối config), `redirect_uri` trỏ vào callback ĐĂNG NHẬP nơi tiêu mất code, và middleware đẩy người đã đăng nhập ra khỏi `/oauth/*`. Đọc + huỷ liên kết vẫn chạy. `pinnedRepos`/`contributionGraph` là `JsonNode` → spec vô dụng, type chép từ GraphQL query. Xem `findings/github.md` | **done (5/5)**   | **done (5/5)**                 | **done (7 hook)** | **4/5** — mặt đọc đủ (stats · pinned · đồ thị · sync · unlink). **Cố ý KHÔNG ship nút liên kết** (B23a)                                    | **done** (`/github`)                        | **n/a** (chưa từng có code legacy)                                                                                                                           | **done** (mặt đọc) |
| matchmaking   | `com.socialapp.matchmaking`   | 5   | **KHÔNG DỰNG ĐƯỢC UI (B24)** — không endpoint nào liệt kê project/position/application (repository cũng rỗng, khác `roadmap`/B21), `createProject` khai `void` và vứt id nó vừa tạo, và endpoint đọc duy nhất (`suggested-candidates`) **luôn 500** do native query thiếu schema. `SuggestedCandidateDto` hẹp là **cố ý** (quyền riêng tư) — đừng bù đắp. Xem `findings/matchmaking.md`                                            | **done (5/5)**   | **done (5/5)**                 | **done (5 hook)** | **hoãn — B24 chặn**                                                                                                                        | **hoãn**                                    | **n/a** (chưa từng có code legacy)                                                                                                                           | data/state đã đo   |
| moderation    | `com.socialapp.moderation`    | 4   | **Domain admin DUY NHẤT có cổng BE thật sự hoạt động**: controller không có `@PreAuthorize` nhưng `SecurityConfig` gác `/v1/api/admin/**` ở tầng URL — ngược hẳn `roadmap` (B20). `decision` là enum 6 mức nhưng service thu về nhị phân và không lưu giá trị đã chọn; từ chối **luôn ghi `HATE_SPEECH`** (B22) và có thể dẫn tới ban 7 ngày. Xem `findings/moderation.md`                                                         | **done (4/4)**   | **done (4/4)**                 | **done (4 hook)** | **done** (3 tab + bộ lọc; `Pagination` mới ở `shared`)                                                                                     | **done** (`/admin/moderation`)              | **done** (`components/moderation/` 5 file, `lib/api/moderation.ts`, `lib/hooks/use-moderation.ts`, 8 type ở `lib/types`, khoá i18n mồ côi)                   | **done**           |
| chat          | `com.socialapp.chat`          | 1   | BE cấp token **Stream Chat**; FE Twilio cũ chết ở P2.7d. BE đã sửa xong R1–R5 (2026-07-27). **R8 đã sửa+verify**: BE upsert cả bạn bè khi cấp token → nhắn được bạn chưa từng mở chat; người ngoài danh sách bạn vẫn lỗi (đúng mong đợi, (i) không phải gác quyền). Vượt trần UI → tách **P2.7a / c-1 / c-2 / d**                                                                                                                  | done (1/1)       | done (1/1)                     | done (1/1)        | **done** (màn `/chats` + cửa sổ nổi app shell)                                                                                             | **done** (`/chats` + shell)                 | **done** (Twilio: 6 component + `lib/twilio/` + route handler + 2 dep)                                                                                       | **done**           |
| newsfeed      | `com.socialapp.newsfeed`      | 1   | 1 endpoint nhưng là **surface đọc lớn nhất app** — tier C nói nhịp checkpoint, không nói khối lượng                                                                                                                                                                                                                                                                                                                                | done (1/1)       | done (1/1)                     | done (1/1)        | done (feed + adapter card)                                                                                                                 | done (`/newsfeed`)                          | done (bookstore giữ cầu tạm → P2.10)                                                                                                                         | done               |
| reputation    | `com.socialapp.reputation`    | 1   |                                                                                                                                                                                                                                                                                                                                                                                                                                    | done (1/1)       | done (1/1)                     | done (1/1)        | done (card + chip)                                                                                                                         | done                                        | n/a (chưa từng có code legacy)                                                                                                                               | done               |
| search        | `com.socialapp.search`        | 1   | 1 endpoint trả **hai** danh sách (người + bài). Sách **không** là danh sách thứ ba — sách khớp nổi lên dưới dạng bài gắn nó. Không cursor/không xếp hạng: `unaccent LIKE` trên 3 bảng Postgres. Xem `findings/search.md`                                                                                                                                                                                                           | done (1/1)       | done (1/1)                     | done (1/1)        | done (`/search` + ô tìm kiếm của shell)                                                                                                    | done (`/search` + shell)                    | done (`components/search/`, `lib/api/search.ts`, `lib/hooks/use-search.ts`, 4 type)                                                                          | done               |
| trending      | `com.socialapp.trending`      | 1   | Nội dung **không do người dùng app viết** — crawler kéo về từ HN/dev.to/GitHub/Reddit/Medium/HBR, Gemini phân loại. Không tạo/sửa/react; mỗi card là link ra ngoài. Phân trang thật (`hasNext`). Xem `findings/trending.md`                                                                                                                                                                                                        | done (1/1)       | done (1/1)                     | done (1/1)        | done (`/trending`: card + bộ lọc + infinite scroll)                                                                                        | done (`/trending`)                          | done (`components/trending/`, `lib/api/trending.ts`, `lib/hooks/use-trending.ts`, 5 type)                                                                    | done               |

Σ 15 domain · 102 endpoint (101 → 102 sau đợt bugfix BE 28–29/07). `cloud` và `common` không có REST controller → không thành
FE feature.

**Mục tiêu thật là 98, không phải 102:** 4 endpoint không dành cho FE gọi —
`POST /v1/api/payments/momo/webhook` (MoMo gọi server-to-server) ·
`GET /v1/api/events/google/callback` (Google redirect thẳng về BE) ·
`GET /v1/api/knowledge/sync/pull` + `POST /v1/api/knowledge/sync/push` (**xác thực bằng Personal
Access Token, JWT phiên bị từ chối — đo ở P2.11a**; dành cho client Obsidian bên ngoài). Xem P4.7.

### DS deviation

Đã tách ra [`ledger/ds-deviations.md`](ledger/ds-deviations.md) (13 dòng).

---

## 2. Bản đồ route → domain (P0.4)

25 page (21 từ P0.4 + `/notifications` ở P2.6cd + `/knowledge` ở P2.11d + `/roadmap` và `/admin/roadmap` ở P2.13d) + 4 layout. Route handler `/api/twilio/token` **đã xoá ở P2.7d** — `src/app/api/` không còn tồn tại. Bảng dưới
suy ra từ **đồ thị import thật**, không
đoán theo tên thư mục: barrel `@/lib/hooks` và `@/lib/api` re-export cả 14 domain nên
nếu đi theo import thô thì trang nào cũng "chạm" mọi domain — phải resolve từng symbol
qua barrel về đúng file định nghĩa mới ra được bảng này.

**Domain chủ** sở hữu shell + layout của trang. Domain góp mặt chỉ export component qua
`index.ts` của mình, không domain nào import internals của domain khác (CLAUDE.md §4).

| route                   | file                                      | domain chủ     | domain góp mặt             | ghi chú                                                                                                                                                                                                                                                                                                                                           |
| ----------------------- | ----------------------------------------- | -------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| `/`                     | `app/page.tsx`                            | —              | —                          | chỉ `redirect('/newsfeed')`                                                                                                                                                                                                                                                                                                                       |
| `/login`                | `app/(auth)/login/page.tsx`               | security       | —                          |                                                                                                                                                                                                                                                                                                                                                   |
| `/register`             | `app/(auth)/register/page.tsx`            | security       | —                          |                                                                                                                                                                                                                                                                                                                                                   |
| `/forgot-password`      | `app/(auth)/forgot-password/page.tsx`     | security       | —                          |                                                                                                                                                                                                                                                                                                                                                   |
| `/reset-password`       | `app/(auth)/reset-password/page.tsx`      | security       | —                          |                                                                                                                                                                                                                                                                                                                                                   |
| `/verify-email`         | `app/(auth)/verify-email/page.tsx`        | security       | —                          |                                                                                                                                                                                                                                                                                                                                                   |
| `/magic-link`           | `app/(auth)/magic-link/page.tsx`          | security       | —                          |                                                                                                                                                                                                                                                                                                                                                   |
| `/magic-login`          | `app/(auth)/magic-login/page.tsx`         | security       | —                          |                                                                                                                                                                                                                                                                                                                                                   |
| `/newsfeed`             | `app/(main)/newsfeed/page.tsx`            | **newsfeed**   | posts, bookstore, security | **span 4 domain** — P3.1. **Không còn legacy nào** sau P2.10d: cầu tạm bookstore đã xoá, prop `renderBookActions` đã bỏ, `features/newsfeed` import `BookActions` thẳng qua barrel `features/bookstore` (§4 cho phép). Page chỉ còn nối `onPosted`                                                                                                |
| `/dashboard`            | `app/(main)/dashboard/page.tsx`           | **security**   | friendships                | **span 3** — P3.3. Xem quyết định bên dưới                                                                                                                                                                                                                                                                                                        |
| `/profile`              | `app/(main)/profile/page.tsx`             | security       | —                          | P3.2 sẽ thêm knowledge, roadmap, github, reputation                                                                                                                                                                                                                                                                                               |
| `/friends`              | `app/(main)/friends/page.tsx`             | —              | —                          | chỉ `redirect('/friends/all')`                                                                                                                                                                                                                                                                                                                    |
| `/friends/all`          | `app/(main)/friends/all/page.tsx`         | friendships    | —                          |                                                                                                                                                                                                                                                                                                                                                   |
| `/friends/requests`     | `app/(main)/friends/requests/page.tsx`    | friendships    | —                          |                                                                                                                                                                                                                                                                                                                                                   |
| `/friends/suggestions`  | `app/(main)/friends/suggestions/page.tsx` | friendships    | —                          |                                                                                                                                                                                                                                                                                                                                                   |
| `/chats`                | `app/(main)/chats/page.tsx`               | **chat**       | friendships                | **span 2 sau P2.7d** — trang chỉ còn `ChatMessenger` + state URL; phần security (tên/avatar mình) không cần nữa vì Stream mang sẵn thành viên channel. friendships vào qua barrel trong `ConversationSidebar`                                                                                                                                     |
| `/search`               | `app/(main)/search/page.tsx`              | search         | reputation                 | **P2.8**: page chỉ giữ `?q=`, `SearchResults` lo phần còn lại. `RepScore` vào qua barrel `features/reputation` (`eliteScore` nằm sẵn trong payload). Kết quả **không bấm được** — BE không có profile công khai, app không có route chi tiết bài                                                                                                  |
| `/trending`             | `app/(main)/trending/page.tsx`            | trending       | —                          | **P2.9**: page chỉ còn tiêu đề + `TrendingList`. **Không feature nào khác góp mặt** — item crawl không có tác giả trong hệ thống nên không identity row, không Elite Score. Extraction test sạch nhất dự án                                                                                                                                       |
| `/notifications`        | `app/(main)/notifications/page.tsx`       | notifications  | —                          | **route mới ở P2.6cd** — domain có 6/6 endpoint nhưng 0 UI nên không có trang nào để rewire. Chuông + badge trên topbar vẫn thuộc app shell → P3.4                                                                                                                                                                                                |
| `/github`               | `app/(main)/github/page.tsx`              | **github**     | security (chỉ lấy `id`)    | **route mới ở P2.14cd** — domain chưa từng có UI. Chỉ mặt ĐỌC; **không có nút liên kết** vì luồng đó bế tắc (B23a). **Nhiều khả năng biến mất ở P3.2** khi `/profile` hấp thụ `GithubStatsCard` — component không phải dời, chỉ đổi chỗ compose                                                                                                   |
| `/knowledge`            | `app/(main)/knowledge/page.tsx`           | **knowledge**  | —                          | **route mới ở P2.11d** — domain có 10 endpoint nhưng 0 UI nên không có trang nào để rewire (cùng tình huống `/notifications` ở P2.6cd). Gom cả 3 mặt: hồ sơ nghề nghiệp · token · thư viện. Hồ sơ **cố ý không đặt ở `/profile`** (trang đó của `security`, lắp ghép ở P3.2) — P3.2 muốn hiện thì import lại component qua barrel, không dời code |
| `/roadmap`              | `app/(main)/roadmap/page.tsx`             | **roadmap**    | —                          | **route mới ở P2.13d** — domain có 8 endpoint nhưng 0 UI nên không có trang nào để rewire (cùng tình huống `/notifications` P2.6cd và `/knowledge` P2.11d). Chỉ mặt DUYỆT; mặt admin nằm ở `/admin/roadmap` vì middleware đẩy mọi phiên `ADMIN` ra khỏi `(main)` (`findings/roadmap.md` §10). Page chỉ giữ `?id=` + dialog                        |
| `/admin/roadmap`        | `app/(admin)/admin/roadmap/page.tsx`      | **roadmap**    | —                          | **route mới ở P2.13d** — hàng chờ duyệt + 2 form soạn thảo. Đặt dưới `(admin)` là **bắt buộc**, không phải lựa chọn: `ADMIN` không mở được route nào ngoài `/admin/**`. Header `(admin)` trước đó **không có nav nào** (chỉ 1 trang) → P2.13d thêm 2 link                                                                                         |
| `/admin/moderation`     | `app/(admin)/admin/moderation/page.tsx`   | **moderation** | —                          | **rewire ở P2.15cd** — page chỉ còn giữ tab đang chọn + cú nhảy sang hàng chờ; 3 tab và bộ lọc nằm trong feature. Nhảy sang bài là **remount** (`key`) chứ không phải đồng bộ prop như legacy                                                                                                                                                     |     |
| `/payment/success`      | `app/payment/success/page.tsx`            | bookstore      | —                          | **P2.10d**: route chỉ còn đọc `?orderId=`; vòng poll có giới hạn + 4 trạng thái nằm trong `PaymentResultPanel` của feature                                                                                                                                                                                                                        |
| ~~`/api/twilio/token`~~ | ~~`app/api/twilio/token/route.ts`~~       | chat           | —                          | **ĐÃ XOÁ ở P2.7d.** Là một backend nằm trong Next.js (tự ký JWT bằng secret Twilio) — trái mục tiêu microservices-ready; BE cấp token Stream thay nó. `src/app/api/` giờ không còn thư mục                                                                                                                                                        |

| layout                   | domain chủ         | ghi chú                                                                                                    |
| ------------------------ | ------------------ | ---------------------------------------------------------------------------------------------------------- |
| `app/layout.tsx`         | —                  | providers, theme                                                                                           |
| `app/(auth)/layout.tsx`  | —                  | thuần trình bày                                                                                            |
| `app/(main)/layout.tsx`  | shared (app shell) | chat đã sang `features/chat` (`ChatClientProvider` + `ChatDock`, P2.7d); còn friendships + security → P3.4 |
| `app/(admin)/layout.tsx` | security           | chặn quyền admin + (từ P2.13d) 2 link nav — trước đó không có điều hướng nào vì `(admin)` chỉ có 1 trang   |

**1 trang span nhiều domain còn phải chờ**: `/dashboard` (3) — P3.3. `/chats` đóng ở P2.7d;
`/newsfeed` **hết legacy ở P2.10d** — vẫn span 4 domain nên assembly cuối vẫn là P3.1, nhưng
không còn gì phải xoá.

### Quyết định: domain chủ của `/dashboard`

WBS để ngỏ "quyết ở P0.4". Trang hiện gồm lời chào + identity (security), thẻ số bạn bè /
gợi ý / lời mời (friendships), và 2 thẻ follower/following gọi vào API không tồn tại.

**Chọn `security`.** Trang là "home của tài khoản đang đăng nhập" — shell và identity
thuộc security, friendships chỉ góp widget qua `index.ts`. Đã cân nhắc cho friendships
sở hữu vì phần lớn widget là bạn bè, nhưng như vậy shell sẽ phải import identity từ
security ngược lại, đúng kiểu phụ thuộc hai chiều mà §4 cấm.

---

---

## 3. Legacy · nhật ký · drift · phát hiện

Đã tách ra, xem bảng "File nào đọc lúc nào" ở đầu file.
